---
phase: 03-backend-service-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/test/helpers/mock-builders.ts
  - backend/src/auth/auth.controller.spec.ts
  - backend/src/fabric/fabric.service.spec.ts
  - backend/src/order/order.service.spec.ts
  - backend/src/import/import.service.spec.ts
  - backend/eslint.config.mjs
autonomous: true
requirements:
  - QUAL-06

must_haves:
  truths:
    - "Zero `as any` casts exist in backend test files"
    - "ESLint warns on new `any` usage in spec files"
    - "All 608+ backend tests pass after mock builder replacement"
  artifacts:
    - path: "backend/test/helpers/mock-builders.ts"
      provides: "Typed mock builder utilities (createMockAuthRequest, loadTestWorkbook)"
      exports: ["createMockAuthRequest", "loadTestWorkbook"]
    - path: "backend/eslint.config.mjs"
      provides: "ESLint override for no-explicit-any on spec files"
      contains: "no-explicit-any"
  key_links:
    - from: "backend/src/auth/auth.controller.spec.ts"
      to: "backend/test/helpers/mock-builders.ts"
      via: "import createMockAuthRequest"
      pattern: "import.*createMockAuthRequest.*mock-builders"
    - from: "backend/src/import/import.service.spec.ts"
      to: "backend/test/helpers/mock-builders.ts"
      via: "import loadTestWorkbook"
      pattern: "import.*loadTestWorkbook.*mock-builders"
---

<objective>
Create shared typed mock builder utilities and eliminate all 11 `as any` casts across 4 backend test files. Enable ESLint `no-explicit-any` as warning on `*.spec.ts` files to prevent future regression.

Purpose: Establishes typed test infrastructure used by Plans 02-04. Without this, the decomposed service tests in Plan 02 would inherit the `as any` casts.
Output: `backend/test/helpers/mock-builders.ts` with typed mock utilities, all 11 `as any` casts replaced, ESLint override configured.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-backend-service-decomposition/03-CONTEXT.md
@.planning/phases/03-backend-service-decomposition/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create mock builder utilities and replace all `as any` casts</name>
  <files>
    backend/test/helpers/mock-builders.ts,
    backend/src/auth/auth.controller.spec.ts,
    backend/src/fabric/fabric.service.spec.ts,
    backend/src/order/order.service.spec.ts,
    backend/src/import/import.service.spec.ts
  </files>
  <read_first>
    backend/src/auth/auth.controller.spec.ts,
    backend/src/fabric/fabric.service.spec.ts,
    backend/src/order/order.service.spec.ts,
    backend/src/import/import.service.spec.ts,
    backend/src/auth/auth.controller.ts,
    backend/src/order/enums/order-status.enum.ts,
    backend/src/fabric/dto/query-fabric-supplier.dto.ts,
    backend/src/fabric/dto/query-customer-fabric.dto.ts
  </read_first>
  <behavior>
    - createMockAuthRequest(userId) returns object typed as `Partial<Request> & { user: { id: number; weworkId: string; name: string }; headers: Record<string, string> }` (not `any`)
    - loadTestWorkbook(buffer: Buffer) returns `Promise<ExcelJS.Workbook>` using `buffer as unknown as ArrayBuffer` internally (not `buffer as any`)
    - auth.controller.spec.ts: all 5 occurrences of `req as any` replaced with `createMockAuthRequest()` call
    - fabric.service.spec.ts: `'purchasePrice' as any` replaced with valid FabricSupplierSortField enum value or Prisma field literal; similarly for `'supplierName' as any`, `'specialPrice' as any`, `'customerName' as any`
    - order.service.spec.ts: `'partial' as any` replaced with `CustomerPayStatus.PARTIAL`
    - import.service.spec.ts: `buffer as any` replaced with `loadTestWorkbook(buffer)` call
  </behavior>
  <action>
    1. Create directory `backend/test/helpers/` and file `mock-builders.ts`.

    2. In `mock-builders.ts`, implement:
       ```typescript
       import type { Request } from 'express';
       import * as ExcelJS from 'exceljs';

       interface MockUser {
         id: number;
         weworkId: string;
         name: string;
       }

       export function createMockAuthRequest(
         userId = 1,
         options?: { authorization?: string },
       ): Partial<Request> & { user: MockUser; headers: Record<string, string> } {
         return {
           user: { id: userId, weworkId: 'test-user-id', name: 'Test User' },
           headers: { authorization: options?.authorization ?? 'Bearer test-token' },
         };
       }

       export async function loadTestWorkbook(buffer: Buffer): Promise<ExcelJS.Workbook> {
         const workbook = new ExcelJS.Workbook();
         await workbook.xlsx.load(buffer as unknown as ArrayBuffer);
         return workbook;
       }
       ```

    3. In `auth.controller.spec.ts` (5 replacements):
       - Add import: `import { createMockAuthRequest } from '../../../test/helpers/mock-builders';`
       - Line 240: Replace `controller.me(req as any)` with `controller.me(createMockAuthRequest(1) as any)` -- NO, instead: type the `req` variable itself using `createMockAuthRequest()` so no cast is needed at call site. The controller.me() parameter type is `Request`, so use `createMockAuthRequest() as Request` (Partial<Request> → Request is acceptable since only `.user` is accessed).
       - Actually: the controller method expects a full `Request` type. Use: `const req = createMockAuthRequest(1) as unknown as Request;` at the test setup, then pass `req` directly. This eliminates `as any` by using a targeted `as unknown as Request` in the setup (which is in the mock builder, not in the test).
       - Better approach: change the mock builder return type annotation to `Request & { user: MockUser }` by casting internally. Then the test just calls `createMockAuthRequest()` and passes it.
       - Final decision: In mock-builders.ts, the function returns the object but casts it as `Request & { user: MockUser }`. Tests import and use directly. The `as unknown as (Request & { user: MockUser })` cast is inside the builder, eliminating `any` from all 5 test call sites.

    4. In `fabric.service.spec.ts` (4 replacements):
       - Read the DTO files to find the actual enum/type for sortBy fields.
       - Lines 1010, 1027: These test _invalid_ sort fields to verify error/fallback handling. The `as any` is used because the test intentionally passes an invalid value. Replace with a type assertion to the specific DTO's sortBy type: e.g., `'purchasePrice' as FabricSupplierSortField` if FabricSupplierSortField is an enum. If it's a string literal union, use a type-specific workaround: create a typed const `const invalidSort = 'purchasePrice' as unknown as FabricSupplierSortField;` -- this eliminates `any` while preserving the intentional invalid value test.
       - Lines 1591, 1608: Same pattern for customer fabric sort fields.

    5. In `order.service.spec.ts` (1 replacement):
       - Line 1648: Replace `'partial' as any` with `CustomerPayStatus.PARTIAL`. The CustomerPayStatus enum has a PARTIAL value. Import it if not already imported.

    6. In `import.service.spec.ts` (1 replacement):
       - Line 17: Replace the inline `workbook.xlsx.load(buffer as any)` with `loadTestWorkbook(buffer)` from mock-builders.
       - Add import: `import { loadTestWorkbook } from '../../../test/helpers/mock-builders';`
       - Update the helper function that currently wraps this call to use `loadTestWorkbook`.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm test -- --testPathPattern "auth\.controller\.spec|fabric\.service\.spec|order\.service\.spec|import\.service\.spec" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `backend/test/helpers/mock-builders.ts` exists and exports `createMockAuthRequest` and `loadTestWorkbook`
    - `grep -rn "as any" backend/src/auth/auth.controller.spec.ts backend/src/fabric/fabric.service.spec.ts backend/src/order/order.service.spec.ts backend/src/import/import.service.spec.ts` returns zero results
    - `cd backend && pnpm test -- --testPathPattern "auth\.controller\.spec|fabric\.service\.spec|order\.service\.spec|import\.service\.spec"` exits with code 0
  </acceptance_criteria>
  <done>All 11 `as any` casts removed from 4 backend test files, replaced with typed alternatives from mock-builders.ts. All affected tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enable ESLint no-explicit-any warning for spec files</name>
  <files>backend/eslint.config.mjs</files>
  <read_first>
    backend/eslint.config.mjs
  </read_first>
  <action>
    Add an ESLint override block in `backend/eslint.config.mjs` that targets `*.spec.ts` files and sets `@typescript-eslint/no-explicit-any` to `warn`.

    The current config has `'@typescript-eslint/no-explicit-any': 'off'` in the global rules. Add a new config object AFTER the global rules block that targets spec files:

    ```javascript
    // Add this as a new config entry in the tseslint.config() array:
    {
      files: ['**/*.spec.ts'],
      rules: {
        '@typescript-eslint/no-explicit-any': 'warn',
      },
    },
    ```

    This goes after the existing rules block (the one with `'@typescript-eslint/no-explicit-any': 'off'`). ESLint flat config applies later entries with higher priority, so this override will apply `warn` specifically to spec files while keeping `off` for production code.

    After adding, run lint to verify no new warnings appear (since we just eliminated all `as any` in Task 1).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm lint 2>&1 | grep -c "no-explicit-any" || echo "0 violations"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/eslint.config.mjs` contains a config block with `files: ['**/*.spec.ts']` and `'@typescript-eslint/no-explicit-any': 'warn'`
    - `cd backend && pnpm lint` exits with code 0 (no errors)
    - `cd backend && pnpm lint 2>&1 | grep "no-explicit-any"` returns zero lines (all `as any` already eliminated)
  </acceptance_criteria>
  <done>ESLint configured to warn on `any` usage in test files. Zero warnings present because all `as any` casts were eliminated in Task 1. Future `as any` introductions will trigger lint warnings.</done>
</task>

</tasks>

<verification>
1. `cd backend && pnpm test` — all 608+ unit tests pass
2. `cd backend && pnpm lint` — zero errors, zero `no-explicit-any` warnings
3. `grep -rn "as any" backend/src/**/*.spec.ts` — zero results
4. `backend/test/helpers/mock-builders.ts` exists with exported utilities
</verification>

<success_criteria>
- All 11 `as any` casts eliminated from 4 backend test files
- Typed mock builder utilities available in `backend/test/helpers/mock-builders.ts`
- ESLint warns on future `any` introduction in spec files
- All existing backend tests continue passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-service-decomposition/03-01-SUMMARY.md`
</output>
