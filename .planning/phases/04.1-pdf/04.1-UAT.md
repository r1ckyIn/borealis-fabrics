---
status: complete
phase: 04.1-pdf
source: 04.1-01-SUMMARY.md, 04.1-02-SUMMARY.md, 04.1-03-SUMMARY.md
started: 2026-03-24T09:10:00Z
updated: 2026-03-24T09:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Voucher Upload Area in Customer Payment Modal
expected: Open an order detail page. Click "Record Payment" on a customer payment. The modal should show a drag-and-drop file upload area (VoucherUploader) for attaching payment vouchers, accepting images, PDFs, and Office documents.
result: pass

### 2. Mandatory Voucher Enforcement
expected: In the payment modal, before uploading any voucher file, the Save/OK button should be DISABLED (greyed out, not clickable). You cannot submit a payment without at least one voucher attached.
result: pass

### 3. Upload Enables Save Button
expected: After uploading at least one voucher file in the payment modal, the Save/OK button becomes ENABLED and clickable.
result: pass

### 4. Historical Voucher Display
expected: On an order that already has payment records with vouchers, the payment section shows a VoucherList below the payment summary cards. Vouchers are grouped by payment record with date headers.
result: pass

### 5. Type-Aware File Actions
expected: In VoucherList, image vouchers show a "Preview" button (opens image preview), PDF vouchers show an "Open" button (opens in new tab), and Office files (.doc/.docx/.xls/.xlsx) show a "Download" button.
result: issue
reported: "Clicking Open on PDF navigates to localhost:5175/orders/<uuid>.pdf instead of opening the file. The file.url from API is just the key (UUID filename), not a full URL. All file actions (Preview/Open/Download) are affected."
severity: blocker

### 6. Supplier Payment Voucher Integration
expected: Supplier payment modal also has the VoucherUploader with the same mandatory enforcement (disabled save without vouchers) and VoucherList displays for supplier payments.
result: skipped
reason: Blocked by Test 5 issue (same file URL root cause)

## Summary

total: 6
passed: 4
issues: 1
pending: 0
skipped: 1

## Gaps

- truth: "File actions work correctly - Preview opens image, Open opens PDF in new tab, Download saves Office files"
  status: failed
  reason: "User reported: Clicking Open on PDF navigates to localhost:5175/orders/<uuid>.pdf instead of opening the file. The file.url from API is just the key (UUID filename), not a full URL. All file actions (Preview/Open/Download) are affected."
  severity: blocker
  test: 5
  root_cause: "getPaymentVouchers returns raw Prisma include with file: true, which gives DB url field (just the key). Unlike FabricService which calls fileService.getFileUrl() to resolve keys to full URLs, OrderPaymentService does not resolve file URLs."
  artifacts:
    - path: "backend/src/order/order-payment.service.ts"
      issue: "getPaymentVouchers returns file.url as raw DB key without resolving via FileService.getFileUrl()"
    - path: "frontend/src/components/business/VoucherList.tsx"
      issue: "window.open(file.url) uses relative key as URL, browser resolves as SPA route"
  missing:
    - "Inject FileService into OrderPaymentService and resolve file URLs in getPaymentVouchers response"
  debug_session: ""
