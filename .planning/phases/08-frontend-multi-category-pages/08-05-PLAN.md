---
phase: 08-frontend-multi-category-pages
plan: 05
type: execute
wave: 4
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - frontend/src/components/layout/__tests__/Sidebar.test.tsx
  - frontend/src/pages/fabrics/FabricListPage.tsx
  - frontend/src/pages/fabrics/FabricDetailPage.tsx
  - frontend/src/pages/fabrics/FabricFormPage.tsx
  - frontend/src/pages/orders/OrderDetailPage.tsx
autonomous: false
requirements:
  - MCAT-10
  - MCAT-11
  - MCAT-12

must_haves:
  truths:
    - "Full test suite passes (frontend build + test + lint + typecheck)"
    - "Sidebar test updated for SubMenu structure"
    - "Fabric pages internal navigation links updated for /products/fabrics prefix"
    - "OrderDetailPage shows product name alongside fabric name for order items"
    - "All existing tests still pass after changes"
  artifacts:
    - path: "frontend/src/components/layout/__tests__/Sidebar.test.tsx"
      provides: "Updated sidebar test for SubMenu"
      contains: "产品管理"
  key_links:
    - from: "frontend/src/pages/fabrics/FabricListPage.tsx"
      to: "/products/fabrics"
      via: "navigate() calls"
      pattern: "/products/fabrics"
---

<objective>
Final integration: update Sidebar test for SubMenu, fix any internal navigation links in fabric pages that still use old /fabrics paths, update OrderDetailPage to show product info for non-fabric order items, and run the full verification suite.

Purpose: Ensures all cross-cutting concerns from Plans 01-04 are wired correctly. Catches any navigation breaks, type mismatches, or test regressions.
Output: Fully green test suite + all pages navigating correctly.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-multi-category-pages/08-CONTEXT.md
@.planning/phases/08-frontend-multi-category-pages/08-01-SUMMARY.md
@.planning/phases/08-frontend-multi-category-pages/08-02-SUMMARY.md
@.planning/phases/08-frontend-multi-category-pages/08-03-SUMMARY.md
@.planning/phases/08-frontend-multi-category-pages/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Sidebar test, fix fabric page navigation, update OrderDetailPage for product items</name>
  <files>
    frontend/src/components/layout/__tests__/Sidebar.test.tsx,
    frontend/src/pages/fabrics/FabricListPage.tsx,
    frontend/src/pages/fabrics/FabricDetailPage.tsx,
    frontend/src/pages/fabrics/FabricFormPage.tsx,
    frontend/src/pages/orders/OrderDetailPage.tsx
  </files>
  <read_first>
    frontend/src/components/layout/__tests__/Sidebar.test.tsx,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/pages/fabrics/FabricListPage.tsx,
    frontend/src/pages/fabrics/FabricDetailPage.tsx,
    frontend/src/pages/fabrics/FabricFormPage.tsx,
    frontend/src/pages/orders/OrderDetailPage.tsx
  </read_first>
  <action>
**Sidebar.test.tsx — Update for new SubMenu structure:**

1. Update test assertions to check for "产品管理" parent menu item instead of "面料管理" as top-level.
2. Add test: SubMenu contains children items (面料, 铁架, 电机, 床垫, 配件).
3. Update selectedKey tests: when pathname is `/products/iron-frames`, verify correct item selected.
4. Update any existing tests that assert on flat menu items — "面料管理" no longer exists as top-level.
5. Keep tests for other menu items (供应商管理, 客户管理, 报价管理, 订单管理, 数据导入) unchanged.

**Fabric pages — Update internal navigation links:**

Search each fabric page for hardcoded `/fabrics` paths in navigate() calls. Replace with `/products/fabrics`:

- `FabricListPage.tsx`:
  - "新增面料" button: `navigate('/fabrics/new')` -> `navigate('/products/fabrics/new')`
  - Row click/view button: `` navigate(`/fabrics/${id}`) `` -> `` navigate(`/products/fabrics/${id}`) ``

- `FabricDetailPage.tsx`:
  - "编辑" button: `` navigate(`/fabrics/${id}/edit`) `` -> `` navigate(`/products/fabrics/${id}/edit`) ``
  - Delete success redirect: `navigate('/fabrics')` -> `navigate('/products/fabrics')`
  - Breadcrumb links: `/fabrics` -> `/products/fabrics`

- `FabricFormPage.tsx`:
  - After save redirect: `navigate('/fabrics')` or `` navigate(`/fabrics/${id}`) `` -> `/products/fabrics` or `/products/fabrics/${id}`
  - Cancel button: same path update
  - Breadcrumb links: same path update

**OrderDetailPage.tsx — Update order item display for product support:**

Find the order items table column that shows `item.fabric?.name` or `item.fabric?.fabricCode`. Update to handle both fabric and product items:

```typescript
// Column: product/fabric name
{ title: '产品', render: (_, item) => {
  if (item.fabric) return `${item.fabric.fabricCode} - ${item.fabric.name}`;
  if (item.product) return `${item.product.productCode} - ${item.product.name}`;
  return '-';
}}
```

Update the unit display to use `item.unit` instead of hardcoded '米':
```typescript
// Quantity column
{ title: '数量', render: (_, item) => `${item.quantity} ${item.unit || '米'}` }
```

If there are fabric detail links in order item rows, update them to use `/products/fabrics/${item.fabricId}` for fabrics or `` `/products/${SUB_CATEGORY_ROUTE_MAP[item.product.subCategory]}/${item.productId}` `` for products.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test --run && pnpm lint && pnpm typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.test.tsx contains "产品管理" assertion
    - FabricListPage.tsx does NOT contain `navigate('/fabrics/` (uses /products/fabrics/)
    - FabricDetailPage.tsx does NOT contain `navigate('/fabrics` without /products prefix
    - FabricFormPage.tsx does NOT contain `navigate('/fabrics` without /products prefix
    - OrderDetailPage.tsx contains `item.product` in render logic
    - OrderDetailPage.tsx contains `item.unit` in quantity display
    - `pnpm build` succeeds
    - `pnpm test --run` succeeds with zero failures
    - `pnpm lint` succeeds
    - `pnpm typecheck` succeeds
  </acceptance_criteria>
  <done>All cross-cutting concerns addressed: Sidebar test updated, fabric page links point to /products/fabrics, OrderDetailPage shows product names for non-fabric items. Full verification suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 8 UI changes</name>
  <files>N/A</files>
  <action>
Present the visual verification checklist to the user for manual testing of all new pages and navigation changes. This task pauses execution until user confirms.

Verification checklist:
1. Start frontend dev server: `cd frontend && pnpm dev`
2. Sidebar: verify "产品管理" parent menu expands to 5 sub-items (面料, 铁架, 电机, 床垫, 配件)
3. Product list: click "铁架" — verify table shows productCode, name, modelNumber, specification, defaultPrice columns
4. Product form: click "新增" on iron frame list — verify form shows 型号 and 规格 fields
5. Fabric redirect: navigate to /fabrics in URL bar — verify redirect to /products/fabrics
6. Order form: create new order — verify unified product search shows fabrics + products with category tags
7. Quote list: verify expandable rows showing QuoteItem details
8. Quote form: create new quote — verify multi-item form with add/remove rows, no supplier fields
9. Quote detail: verify checkbox selection on items and "转化为订单" button
  </action>
  <verify>User confirms "approved" or describes issues</verify>
  <done>User has visually verified all Phase 8 UI changes work correctly in the browser.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm build` — successful build
2. `cd frontend && pnpm test --run` — all tests pass
3. `cd frontend && pnpm lint` — no warnings
4. `cd frontend && pnpm typecheck` — no errors
5. Manual visual verification of all new pages
</verification>

<success_criteria>
- Full frontend verification suite passes (build + test + lint + typecheck)
- Sidebar SubMenu works with correct selectedKey highlighting
- Fabric pages navigate correctly via /products/fabrics paths
- OrderDetailPage displays product names for non-fabric items
- Human verification confirms all new UI works as expected
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-multi-category-pages/08-05-SUMMARY.md`
</output>
