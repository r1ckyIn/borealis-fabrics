---
phase: 17-domain-ssl-launch
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified: []
autonomous: false
requirements: [DEPLOY-03, DEPLOY-04, DEPLOY-05]
must_haves:
  truths:
    - "System accessible via https://<domain> with valid SSL certificate"
    - "HTTP requests to domain redirect to HTTPS (301)"
    - "IP direct access redirects to https://<domain> (301)"
    - "WeChat Work OAuth login works: QR scan → callback → JWT cookie → authenticated"
    - "ALLOW_DEV_LOGIN removed from production .env, dev login button gone"
    - "Auth cookies have Secure + HttpOnly flags"
    - "Certbot auto-renewal cron is configured"
  artifacts: []
  key_links:
    - from: "browser"
      to: "https://<domain>"
      via: "DNS A record + Nginx HTTPS"
      pattern: "SSL certificate verify ok"
    - from: "WeChat Work QR scan"
      to: "https://<domain>/api/v1/auth/wework/callback"
      via: "OAuth redirect"
      pattern: "bf_auth_token.*Secure.*HttpOnly"
---

<objective>
Deploy Phase B: domain + HTTPS + WeChat Work OAuth on production server. This is a human-gated checkpoint that executes the cutover after the user completes domain purchase and ICP filing.

Purpose: The code changes (Plans 01-02) are merged to main. This plan guides the user through the actual production cutover following the DEPLOY.md Phase B instructions.

Output: Production system live on HTTPS with WeChat Work OAuth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-domain-ssl-launch/17-CONTEXT.md
@docs/DEPLOY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User completes domain purchase + ICP filing</name>
  <files>none</files>
  <action>
    Domain registration and ICP filing are prerequisites that only the user can complete.
    These typically take 1-4 weeks for ICP approval.

    Steps for the user:
    1. Purchase domain on Tencent Cloud (preference: u2living-related per D-01)
    2. Complete ICP filing through Tencent Cloud Console > ICP Filing
    3. Wait for ICP approval
    4. Add DNS A record pointing domain to server IP 119.29.82.146
    5. Add port 443 (HTTPS) to Lighthouse firewall rules (Console > Lighthouse > Instance > Firewall > Add Rule)
  </action>
  <verify>User provides the confirmed domain name and confirms DNS + firewall are configured</verify>
  <done>Domain purchased, ICP approved, DNS A record points to 119.29.82.146, port 443 open in Lighthouse firewall</done>
  <resume-signal>Provide the domain name (e.g., "domain is u2living.cn, ICP approved, DNS configured")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute Phase B cutover and verify</name>
  <files>none</files>
  <action>
    Plans 17-01 and 17-02 prepared all code and config:
    - Dev login completely removed from backend and frontend
    - Nginx config ready with HTTPS server blocks (using DOMAIN placeholder)
    - docker-compose.prod.yml ready with port 443 + cert volumes
    - DEPLOY.md has complete step-by-step Phase B instructions

    Claude will assist with the server-side cutover following DEPLOY.md sections 3.2-3.7:
    1. SSH to server, replace DOMAIN placeholder in nginx config with actual domain
    2. Install certbot, obtain SSL certificate via webroot
    3. Configure WeChat Work trusted domain + OAuth callback in admin console
    4. Update backend .env (WEWORK_REDIRECT_URI, CORS_ORIGINS, BASE_URL, FORCE_HTTPS_COOKIES, BOSS_WEWORK_IDS)
    5. Pull latest code, rebuild frontend, restart containers
    6. Set up certbot auto-renewal cron
  </action>
  <verify>
    Verify all of these on production:
    - curl -sv https://DOMAIN/health returns 200 with valid SSL cert
    - curl -sv http://DOMAIN/ returns 301 redirect to HTTPS
    - curl -sv http://119.29.82.146/ returns 301 redirect to domain
    - Open browser, click WeChat Work login, scan QR code, verify authenticated
    - Check browser DevTools: auth cookie has Secure + HttpOnly flags
    - certbot renew --dry-run succeeds
  </verify>
  <done>
    System accessible via HTTPS with valid certificate. HTTP and IP access redirect to HTTPS domain. WeChat Work OAuth login works end-to-end. Auth cookies are Secure + HttpOnly. Certbot auto-renewal configured.
  </done>
  <resume-signal>Type "Phase B verified" with results, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
Phase B is fully verified when:
1. HTTPS is working with valid Let's Encrypt certificate
2. HTTP-to-HTTPS redirect works for both domain and IP access
3. WeChat Work OAuth login completes successfully
4. Auth cookies are Secure + HttpOnly
5. Certbot auto-renewal is configured and dry-run passes
6. No dev login code or buttons remain in production
</verification>

<success_criteria>
1. `curl -sv https://<domain>/health` returns 200 with "SSL certificate verify ok"
2. `curl -sv http://<domain>/` returns 301 to https://<domain>
3. `curl -sv http://119.29.82.146/` returns 301 to https://<domain>
4. WeChat Work QR scan login works end-to-end
5. Browser DevTools shows bf_auth_token cookie with Secure and HttpOnly flags
6. `certbot renew --dry-run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-domain-ssl-launch/17-03-SUMMARY.md`
</output>
