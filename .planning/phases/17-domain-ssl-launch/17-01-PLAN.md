---
phase: 17-domain-ssl-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/auth/auth.controller.ts
  - backend/src/auth/auth.service.ts
  - backend/src/config/configuration.ts
  - backend/src/auth/auth.controller.spec.ts
  - backend/src/auth/auth.service.spec.ts
  - backend/test/auth.e2e-spec.ts
  - backend/.env.production.example
  - frontend/src/pages/auth/LoginPage.tsx
  - frontend/src/api/auth.api.ts
  - frontend/src/pages/auth/__tests__/LoginPage.test.tsx
  - frontend/src/test/integration/auth-flow.integration.test.tsx
  - frontend/.env.example
autonomous: true
requirements: [DEPLOY-05]
must_haves:
  truths:
    - "devLogin endpoint no longer exists in backend (404 on POST /auth/dev/login)"
    - "Dev Login button does not render on LoginPage in any environment"
    - "ALLOW_DEV_LOGIN and VITE_ALLOW_DEV_LOGIN do not appear in any source file"
    - "configuration.ts always requires WeWork env vars in production (no ALLOW_DEV_LOGIN guard)"
    - "All existing auth tests pass after dev login code removal"
  artifacts:
    - path: "backend/src/auth/auth.controller.ts"
      provides: "Auth controller without devLogin endpoint"
      contains: "weWorkLogin"
    - path: "backend/src/auth/auth.service.ts"
      provides: "Auth service without devLogin method"
      contains: "handleOAuthCallback"
    - path: "backend/src/config/configuration.ts"
      provides: "Production config with WeWork vars always required"
      contains: "WEWORK_CORP_ID"
    - path: "frontend/src/pages/auth/LoginPage.tsx"
      provides: "Login page with only WeWork login button"
      contains: "WechatWorkOutlined"
    - path: "frontend/src/api/auth.api.ts"
      provides: "Auth API without devLogin function"
      contains: "getWeworkLoginUrl"
  key_links:
    - from: "backend/src/config/configuration.ts"
      to: "backend/src/auth/auth.service.ts"
      via: "WeWork config always validated in production"
      pattern: "requiredVars.*WEWORK_CORP_ID"
---

<objective>
Completely remove all ALLOW_DEV_LOGIN / dev login code from backend and frontend per user decision D-03.

Purpose: Dev login was a Phase A temporary measure for IP+HTTP access. Phase B (domain+HTTPS) uses WeChat Work OAuth exclusively. Complete code deletion ensures no dead code paths remain and configuration.ts properly enforces WeWork env vars in production.

Output: Clean auth codebase with dev login fully removed, all tests updated and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-domain-ssl-launch/17-CONTEXT.md
@.planning/phases/17-domain-ssl-launch/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/src/auth/auth.controller.ts (current):
- Lines 108-136: `devLogin` method — DELETE entirely
- Lines 1-11: imports — remove `ForbiddenException`, `Post`, `HttpCode`, `HttpStatus` if only used by devLogin
- Line 27: `LoginResponseDto` import — keep (used by other methods? No — only devLogin returns it. Check: weWorkCallback returns void via redirect. So LoginResponseDto import can be removed from controller)

From backend/src/auth/auth.service.ts (current):
- Lines 118-139: `devLogin` method — DELETE entirely
- Lines 119-122: ALLOW_DEV_LOGIN check — part of devLogin, DELETE with method

From backend/src/config/configuration.ts (current):
- Lines 15-23: ALLOW_DEV_LOGIN conditional guard — REMOVE the if-block, always push WeWork vars
- After removal, requiredVars should always include WEWORK_CORP_ID, WEWORK_SECRET, WEWORK_AGENT_ID, WEWORK_REDIRECT_URI

From frontend/src/pages/auth/LoginPage.tsx (current):
- Line 7: `CodeOutlined` import — REMOVE
- Line 13: `devLogin` import — REMOVE
- Line 28: `devLoading` state — REMOVE
- Lines 45-56: `handleDevLogin` function — REMOVE
- Lines 128-148: Dev Login button JSX block — REMOVE
- Line 131: `Divider` import — REMOVE (only used by dev login section)

From frontend/src/api/auth.api.ts (current):
- Lines 28-30: `devLogin` function — REMOVE
- Line 34: `devLogin` in authApi object — REMOVE

From frontend/.env.example (current):
- Line 8: `# VITE_ALLOW_DEV_LOGIN=true` — REMOVE

From backend/.env.production.example (current):
- Lines 49-50: `ALLOW_DEV_LOGIN=true` section — REMOVE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dev login from backend (controller, service, configuration, tests)</name>
  <files>
    backend/src/auth/auth.controller.ts,
    backend/src/auth/auth.service.ts,
    backend/src/config/configuration.ts,
    backend/src/auth/auth.controller.spec.ts,
    backend/src/auth/auth.service.spec.ts,
    backend/test/auth.e2e-spec.ts,
    backend/.env.production.example
  </files>
  <read_first>
    backend/src/auth/auth.controller.ts,
    backend/src/auth/auth.service.ts,
    backend/src/config/configuration.ts,
    backend/src/auth/auth.controller.spec.ts,
    backend/src/auth/auth.service.spec.ts,
    backend/test/auth.e2e-spec.ts,
    backend/.env.production.example
  </read_first>
  <action>
    **Per D-03: Completely delete all ALLOW_DEV_LOGIN-related code. No preservation.**

    **auth.controller.ts:**
    1. Remove the entire `devLogin` method (lines 108-136 — from `@Throttle` decorator through the closing brace)
    2. Remove unused imports that were only needed by devLogin:
       - `ForbiddenException` from '@nestjs/common' (check: not used elsewhere in file — REMOVE)
       - `Post` from '@nestjs/common' — keep if used by `logout` (it IS used by logout @Post('logout'))
       - `HttpCode` from '@nestjs/common' — keep if used by `logout` (it IS used by logout @HttpCode)
       - `HttpStatus` from '@nestjs/common' — keep if used by `logout` (it IS used by logout @HttpCode(HttpStatus.OK))
       - `LoginResponseDto` from './dto' — only used as devLogin return type — REMOVE from import
    3. Remove `devLogin: jest.Mock` will be handled in spec file below
    4. Update JSDoc for the class — remove any mention of "dev login"

    **auth.service.ts:**
    1. Remove the entire `devLogin` method (lines 118-139)
    2. Remove `UnauthorizedException` import ONLY if not used elsewhere — check: it IS used in `handleOAuthCallback` (line 86) and `getWeWorkAccessToken` (line 203) and `getWeWorkUserInfo` (line 229). Keep `UnauthorizedException`.
    3. No other imports need removal.

    **configuration.ts:**
    1. Remove the ALLOW_DEV_LOGIN conditional block (lines 15-23). Replace with unconditional push:
       ```typescript
       const validateProductionConfig = () => {
         if (process.env.NODE_ENV === 'production') {
           const requiredVars = [
             'JWT_SECRET',
             'DATABASE_URL',
             'REDIS_URL',
             'COS_SECRET_ID',
             'COS_SECRET_KEY',
             'COS_BUCKET',
             'COS_REGION',
             'CORS_ORIGINS',
             // WeChat Work OAuth (always required in production)
             'WEWORK_CORP_ID',
             'WEWORK_SECRET',
             'WEWORK_AGENT_ID',
             'WEWORK_REDIRECT_URI',
           ];
       ```
    2. Remove the comment about "WeChat Work vars only required when dev login is NOT enabled"

    **auth.controller.spec.ts:**
    1. Remove the entire `describe('devLogin', ...)` block (lines 203-315)
    2. Remove `ForbiddenException` import (line 2) — only used in devLogin tests
    3. Remove `devLogin: jest.Mock` from the authService mock object (line 19 area)
    4. Keep all other describe blocks (weWorkLogin, weWorkCallback, me, logout) unchanged

    **auth.service.spec.ts:**
    1. Remove the entire `describe('devLogin', ...)` block (lines 275-343)
    2. No import changes needed (UnauthorizedException used in other test blocks)

    **auth.e2e-spec.ts:**
    1. Remove `weworkId: 'dev-user'` from mockUser (line 40) — change to a real-looking weworkId like `'test-wework-001'`
    2. Check for any devLogin-related test blocks — there are none in the e2e spec (devLogin was only tested in unit tests)
    3. In `BOSS_WEWORK_IDS` references: none found in e2e spec, but verify by reading

    **backend/.env.production.example:**
    1. Remove the entire Phase A section (lines 49-50):
       ```
       # --- Phase A: Dev Login (remove in Phase B when domain + OAuth is ready) ---
       ALLOW_DEV_LOGIN=true
       ```
    2. Uncomment and activate the FORCE_HTTPS_COOKIES line (lines 52-53):
       Change from:
       ```
       # --- Phase B: HTTPS Cookies (set to true after SSL is configured) ---
       # FORCE_HTTPS_COOKIES=true
       ```
       To:
       ```
       # --- HTTPS Cookies (enable after SSL is configured) ---
       FORCE_HTTPS_COOKIES=true
       ```
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm build && pnpm test -- --testPathPattern=auth && pnpm lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "devLogin\|ALLOW_DEV_LOGIN\|dev-user" backend/src/` returns zero matches (excluding node_modules)
    - `grep -r "devLogin" backend/src/auth/auth.controller.spec.ts` returns zero matches
    - `grep -r "devLogin" backend/src/auth/auth.service.spec.ts` returns zero matches
    - `grep "ALLOW_DEV_LOGIN" backend/.env.production.example` returns zero matches
    - `grep "FORCE_HTTPS_COOKIES=true" backend/.env.production.example` returns one match (uncommented)
    - `grep "WEWORK_CORP_ID" backend/src/config/configuration.ts` returns a match (always in requiredVars)
    - `grep -c "ALLOW_DEV_LOGIN" backend/src/config/configuration.ts` returns 0
    - `cd backend && pnpm test -- --testPathPattern=auth` exits 0
    - `cd backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    All backend dev login code deleted. configuration.ts always requires WeWork vars in production. All backend auth tests pass. No ALLOW_DEV_LOGIN references remain in backend source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dev login from frontend (LoginPage, auth API, tests, env)</name>
  <files>
    frontend/src/pages/auth/LoginPage.tsx,
    frontend/src/api/auth.api.ts,
    frontend/src/pages/auth/__tests__/LoginPage.test.tsx,
    frontend/src/test/integration/auth-flow.integration.test.tsx,
    frontend/.env.example
  </files>
  <read_first>
    frontend/src/pages/auth/LoginPage.tsx,
    frontend/src/api/auth.api.ts,
    frontend/src/pages/auth/__tests__/LoginPage.test.tsx,
    frontend/src/test/integration/auth-flow.integration.test.tsx,
    frontend/.env.example
  </read_first>
  <action>
    **Per D-03: Completely delete all VITE_ALLOW_DEV_LOGIN / dev login code from frontend.**

    **LoginPage.tsx:**
    1. Remove `CodeOutlined` from the icons import (line 7): change to `import { WechatWorkOutlined } from '@ant-design/icons';`
    2. Remove `Divider` from antd imports (line 9): change to `import { Button, Card, Space, Typography, message } from 'antd';` — wait, check if `message` is still used. `message` was used in handleDevLogin catch. After removing handleDevLogin, `message` is unused. REMOVE `message` from imports too.
    3. Remove `useState` import (line 10) — check: is useState used for anything else? No, only `devLoading` state. REMOVE `useState` from react import. The import becomes: `import { Navigate, useLocation, useNavigate } from 'react-router-dom';` — wait, useState comes from 'react' not react-router-dom. Change line 10 from `import { useState } from 'react';` to nothing (delete the line).
    4. Remove `devLogin` from the auth.api import (line 13): change to `import { getWeworkLoginUrl } from '@/api/auth.api';`
    5. Remove `useNavigate` import and `navigate` variable — check: useNavigate was used by handleDevLogin. Is it used elsewhere? No. But useLocation IS still used (for `from` state). So: remove `useNavigate` from react-router-dom import, remove `const navigate = useNavigate();` line.
       Wait — re-check. `Navigate` component is used for redirect. `useNavigate` is the hook. Only handleDevLogin uses `navigate()`. So remove useNavigate from import and the `navigate` const.
       Updated import: `import { Navigate, useLocation } from 'react-router-dom';`
    6. Remove `const setUser = useAuthStore((s) => s.setUser);` — only used by handleDevLogin
    7. Remove `const [devLoading, setDevLoading] = useState(false);` line
    8. Remove the entire `handleDevLogin` async function (lines 45-56)
    9. Remove the entire dev login JSX block (lines 128-148) — from `{/* Dev login: ... */}` through the closing `</>` and `)}` 
    10. Keep the WeWork login button and all other UI unchanged
    11. After cleanup, the component should only have: useLocation, useIsAuthenticated, useIsInitializing, from state logic, isInitializing guard, isAuthenticated redirect, handleLogin (WeWork), and the Card with WeWork button.

    **auth.api.ts:**
    1. Remove the `devLogin` function (lines 28-30)
    2. Remove `devLogin` from the `authApi` object (line 34)
    3. The file should export: `getWeworkLoginUrl`, `getCurrentUser`, `logout`, and `authApi` (without devLogin)

    **LoginPage.test.tsx:**
    1. Remove `mockDevLogin` variable and mock (lines 25-26 area)
    2. In the `vi.mock('@/api/auth.api', ...)` call, remove `devLogin: (...args: unknown[]) => mockDevLogin(...args),` line
    3. Remove the test "should render Dev Login button in development mode" (lines 133-142)
    4. Remove the test "should call devLogin API and navigate on Dev Login click" (lines 144-163)
    5. Remove the test "should show error message when dev login fails" (lines 165-179)
    6. Keep all other tests: render login page, redirect on click, redirect authenticated user, redirect to original location, return null during initialization
    7. Remove unused imports: `userEvent` may still be needed for the "redirect to home when clicking login button" test. Check: yes, line 73 uses `userEvent.setup()`. Keep it.

    **auth-flow.integration.test.tsx:**
    1. In the vi.mock block (lines 26-31), remove `devLogin: vi.fn(),` line
    2. In the destructured import (line 34-35), remove `devLogin` from the destructured variables
    3. Remove the test "Dev Login sets auth state and navigates to home" (lines 129-146)
    4. Remove the test "Dev Login failure shows error message" (lines 148-159)
    5. Keep all other tests: ProtectedRoute tests, LoginPage render/redirect tests, OAuthCallback tests

    **frontend/.env.example:**
    1. Remove the last 2 lines:
       ```
       # Phase A: Show dev login button in production (remove in Phase B)
       # VITE_ALLOW_DEV_LOGIN=true
       ```
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "devLogin\|ALLOW_DEV_LOGIN\|CodeOutlined\|handleDevLogin\|devLoading" frontend/src/` returns zero matches
    - `grep "VITE_ALLOW_DEV_LOGIN" frontend/.env.example` returns zero matches
    - `grep -c "Dev Login" frontend/src/pages/auth/__tests__/LoginPage.test.tsx` returns 0
    - `grep -c "devLogin" frontend/src/test/integration/auth-flow.integration.test.tsx` returns 0
    - `grep -c "devLogin" frontend/src/api/auth.api.ts` returns 0
    - LoginPage.tsx imports do NOT contain `CodeOutlined`, `Divider`, `message`, `useState`, `useNavigate`
    - `cd frontend && pnpm test` exits 0
    - `cd frontend && pnpm typecheck` exits 0
    - `cd frontend && pnpm build` exits 0
  </acceptance_criteria>
  <done>
    All frontend dev login code deleted. LoginPage shows only WeWork login button. All frontend tests pass. No VITE_ALLOW_DEV_LOGIN or devLogin references remain in frontend source.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Full backend verification
cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm build && pnpm test && pnpm lint && npx tsc --noEmit

# Full frontend verification
cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck

# Global grep: zero ALLOW_DEV_LOGIN references anywhere
grep -r "ALLOW_DEV_LOGIN" --include="*.ts" --include="*.tsx" --include="*.env*" /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/{backend,frontend}/ | grep -v node_modules | grep -v dist
```
</verification>

<success_criteria>
1. Zero references to `ALLOW_DEV_LOGIN`, `VITE_ALLOW_DEV_LOGIN`, `devLogin`, `dev-user` in backend/frontend source code
2. `configuration.ts` always requires `WEWORK_CORP_ID`, `WEWORK_SECRET`, `WEWORK_AGENT_ID`, `WEWORK_REDIRECT_URI` in production
3. LoginPage only renders the WeChat Work login button (no Dev Login button in any environment)
4. All backend tests pass: `pnpm build && pnpm test && pnpm lint && npx tsc --noEmit`
5. All frontend tests pass: `pnpm build && pnpm test && pnpm lint && pnpm typecheck`
</success_criteria>

<output>
After completion, create `.planning/phases/17-domain-ssl-launch/17-01-SUMMARY.md`
</output>
