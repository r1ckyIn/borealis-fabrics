---
phase: 09-real-data-testing
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - scripts/run-full-import-test.ts
  - scripts/verify-page-stability.ts
  - scripts/manual-entry-test-guide.md
autonomous: false
requirements:
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07

must_haves:
  truths:
    - "All 13 xlsx files from /Users/qinyuan/Desktop/铂润测试资料/ have been tested through the import system"
    - "Manual entry of a complete supplier-customer-fabric-quote-order chain succeeds with real data"
    - "Fabric price list import produces records matching source data (record count, field values)"
    - "Iron frame/motor price list import produces records matching source data"
    - "Purchase order import creates product records from 海宁优途-采购单 files"
    - "Sales contract import creates order records from 购销合同 and 客户订单 files"
    - "All frontend pages load correctly after bulk data import (no 500 errors, no broken links)"
  artifacts:
    - path: "scripts/run-full-import-test.ts"
      provides: "End-to-end import test script: runs all 13 xlsx files in correct order"
      min_lines: 100
    - path: "scripts/verify-page-stability.ts"
      provides: "Automated page stability checker: hits all API routes, checks for errors"
      min_lines: 60
    - path: "scripts/manual-entry-test-guide.md"
      provides: "Step-by-step manual entry test guide with real data values"
      min_lines: 40
  key_links:
    - from: "scripts/run-full-import-test.ts"
      to: "backend/src/import/import.controller.ts"
      via: "HTTP calls to all import endpoints"
      pattern: "/api/v1/import/(fabrics|suppliers|products|purchase-orders|sales-contracts)"
    - from: "scripts/verify-page-stability.ts"
      to: "frontend/src/routes/index.tsx"
      via: "Traverses all registered frontend routes"
      pattern: "/api/v1/(fabrics|products|suppliers|customers|orders|quotes)"
---

<objective>
Execute the complete end-to-end validation: run data preparation scripts, import all 13 xlsx files, perform manual entry testing, and verify page stability after bulk import.

Purpose: This is the actual testing phase -- Plans 01 and 02 built the tools, this plan uses them to validate the entire system with real company data. DATA-03 through DATA-07 are all validated here.

Output: Import test scripts, page stability checker, manual entry test guide, and a checkpoint for user to verify imported data in the UI.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-data-testing/09-CONTEXT.md
@.planning/phases/09-real-data-testing/09-RESEARCH.md
@.planning/phases/09-real-data-testing/09-01-SUMMARY.md
@.planning/phases/09-real-data-testing/09-02-SUMMARY.md

<interfaces>
<!-- Executor needs these to build the test scripts -->

Backend API endpoints (all prefixed with /api/v1):
```
POST /import/fabrics          -- FabricImportStrategy (template format)
POST /import/suppliers        -- SupplierImportStrategy (template format)
POST /import/products         -- ProductImportStrategy (template format)
POST /import/purchase-orders  -- PurchaseOrderImportStrategy (采购单 format)
POST /import/sales-contracts  -- SalesContractImportStrategy (购销合同/客户订单 format)
GET  /import/templates/fabrics   -- download template
GET  /import/templates/suppliers -- download template
GET  /import/templates/products  -- download template

POST /suppliers       -- create supplier
POST /customers       -- create customer
POST /fabrics         -- create fabric
POST /quotes          -- create quote
POST /quotes/:id/items -- add quote item
POST /orders          -- create order
POST /orders/:id/items -- add order item

GET /suppliers?page=1&pageSize=10  -- list suppliers
GET /customers?page=1&pageSize=10  -- list customers
GET /fabrics?page=1&pageSize=10    -- list fabrics
GET /products?page=1&pageSize=10   -- list products
GET /orders?page=1&pageSize=10     -- list orders
GET /quotes?page=1&pageSize=10     -- list quotes
```

From frontend/src/routes/index.tsx (all frontend routes):
```
/products/fabrics, /products/fabrics/new, /products/fabrics/:id, /products/fabrics/:id/edit
/products/:category, /products/:category/new, /products/:category/:id, /products/:category/:id/edit
/suppliers, /suppliers/new, /suppliers/:id, /suppliers/:id/edit
/customers, /customers/new, /customers/:id, /customers/:id/edit
/quotes, /quotes/new, /quotes/:id, /quotes/:id/edit
/orders, /orders/new, /orders/:id, /orders/:id/edit
/import
```

Test data files (all 13 xlsx):
```
/Users/qinyuan/Desktop/铂润测试资料/面料价格明细2025.8.15.xlsx
/Users/qinyuan/Desktop/铂润测试资料/铁架电机价格2025.xlsx
/Users/qinyuan/Desktop/铂润测试资料/海宁优途-采购单 2025.11.26.xlsx
/Users/qinyuan/Desktop/铂润测试资料/海宁优途-采购单 2026.03.06.xlsx
/Users/qinyuan/Desktop/铂润测试资料/海宁优途-采购单 2026.3.13.xlsx
/Users/qinyuan/Desktop/铂润测试资料/购销合同SH20260129-03 77854 77855 (U16-116面料).xlsx
/Users/qinyuan/Desktop/铂润测试资料/购销合同SH20260131-02（U18-111铁架）.xlsx
/Users/qinyuan/Desktop/铂润测试资料/77947 U19-156 U18-111面料.xlsx
/Users/qinyuan/Desktop/铂润测试资料/77947 U19-156 U18-111铁架.xlsx
/Users/qinyuan/Desktop/铂润测试资料/77955 MABLE U72面料.xlsx
/Users/qinyuan/Desktop/铂润测试资料/77955 MABLE U72铁架.xlsx
/Users/qinyuan/Desktop/铂润测试资料/77962 SYDNEY面料.xlsx
/Users/qinyuan/Desktop/铂润测试资料/77962 SYDNEY铁架.xlsx
```

Import result type:
```typescript
interface ImportResult {
  successCount: number;
  skippedCount: number;
  failureCount: number;
  failures: Array<{ rowNumber: number; identifier: string; reason: string }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end import test script + page stability checker + manual entry guide</name>
  <files>scripts/run-full-import-test.ts, scripts/verify-page-stability.ts, scripts/manual-entry-test-guide.md</files>
  <read_first>
    - scripts/prepare-fabric-pricelist.ts (from Plan 01, understand output location)
    - scripts/prepare-product-pricelist.ts (from Plan 01, understand output location)
    - backend/src/import/import.controller.ts (all endpoint paths)
    - frontend/src/routes/index.tsx (all frontend routes)
    - backend/src/supplier/dto/create-supplier.dto.ts (supplier creation fields)
    - backend/src/customer/dto/create-customer.dto.ts (customer creation fields)
  </read_first>
  <action>
**1. Create `scripts/run-full-import-test.ts`** -- orchestrates the complete two-stage import of all 13 files.

The script:
- Requires the backend to be running at `http://localhost:3000`
- Uses native `fetch` (Node 18+) or `axios` (from backend/node_modules) to call API endpoints
- Authenticates first (POST /api/v1/auth/login with test credentials, or use cookie from existing session)

**Execution flow (two-stage import, ordered):**

Stage 1: Base data import (must complete before Stage 2)
```
Step 1: Run prepare-fabric-pricelist.ts (generates scripts/output/fabrics-prepared.xlsx)
Step 2: Run prepare-product-pricelist.ts (generates scripts/output/products-prepared.xlsx)
Step 3: Import suppliers via POST /api/v1/import/suppliers (if supplier template exists in output)
         -- OR create key suppliers manually via API calls:
         POST /api/v1/suppliers body: { companyName: '海宁优途', contactName: 'TBD', phone: 'TBD' }
         POST /api/v1/suppliers body: { companyName: 'Miraggo HomeLiving', contactName: 'TBD', phone: 'TBD' }
         (and other suppliers found in sheet names from 面料价格明细)
Step 4: Import fabrics via POST /api/v1/import/fabrics (upload scripts/output/fabrics-prepared.xlsx)
         Log result: successCount, skippedCount, failureCount, first 5 failures
Step 5: Import products via POST /api/v1/import/products (upload scripts/output/products-prepared.xlsx)
         Log result: successCount, skippedCount, failureCount, first 5 failures
```

Stage 2: Order/document import (depends on Stage 1 entities existing)
```
Step 6: Import 采购单 files (3 files):
         POST /api/v1/import/purchase-orders with each file
         - 海宁优途-采购单 2025.11.26.xlsx
         - 海宁优途-采购单 2026.03.06.xlsx
         - 海宁优途-采购单 2026.3.13.xlsx
         Log result for each

Step 7: Import 购销合同 files (2 files):
         POST /api/v1/import/sales-contracts with each file
         - 购销合同SH20260129-03 77854 77855 (U16-116面料).xlsx
         - 购销合同SH20260131-02（U18-111铁架）.xlsx
         Log result for each

Step 8: Import 客户订单 files (6 files):
         POST /api/v1/import/sales-contracts with each file (same endpoint as 购销合同)
         - 77947 U19-156 U18-111面料.xlsx
         - 77947 U19-156 U18-111铁架.xlsx
         - 77955 MABLE U72面料.xlsx
         - 77955 MABLE U72铁架.xlsx
         - 77962 SYDNEY面料.xlsx
         - 77962 SYDNEY铁架.xlsx
         Log result for each
```

Final summary:
```
Step 9: Print summary table:
         | File | Success | Skipped | Failed |
         |------|---------|---------|--------|
         | fabrics-prepared.xlsx | N | N | N |
         | ... | ... | ... | ... |
         Total: X succeeded, Y skipped, Z failed across all 13 files
```

Error handling: If any step fails completely (network error, 500), log the error and continue with next file. Do not abort the entire script on a single file failure.

Auth handling: Try to read a JWT token from environment variable `TEST_AUTH_TOKEN`, or attempt login with `ADMIN_EMAIL`/`ADMIN_PASSWORD` env vars, or fall back to creating a temporary auth cookie. Document the auth setup in script comments.

**2. Create `scripts/verify-page-stability.ts`** -- automated smoke test that hits all API list endpoints and checks for errors.

The script:
- Calls each list API endpoint with default pagination (page=1, pageSize=10)
- Checks response status is 200 (not 500, not 404)
- Checks response body has expected shape (items array, total count)
- Logs pass/fail for each endpoint

API endpoints to check:
```
GET /api/v1/fabrics?page=1&pageSize=10
GET /api/v1/products?page=1&pageSize=10
GET /api/v1/suppliers?page=1&pageSize=10
GET /api/v1/customers?page=1&pageSize=10
GET /api/v1/orders?page=1&pageSize=10
GET /api/v1/quotes?page=1&pageSize=10
```

For each endpoint with results, also fetch the first detail page:
```
GET /api/v1/fabrics/{firstId}
GET /api/v1/products/{firstId}
GET /api/v1/suppliers/{firstId}
GET /api/v1/customers/{firstId}
GET /api/v1/orders/{firstId}
GET /api/v1/quotes/{firstId}
```

Summary output:
```
Page Stability Check Results:
  /fabrics list ............ PASS (42 records)
  /fabrics/1 detail ........ PASS
  /products list ........... PASS (156 records)
  ...
  Total: 12/12 PASS, 0 FAIL
```

**3. Create `scripts/manual-entry-test-guide.md`** -- step-by-step guide for manual CRUD testing with real data.

The guide covers (DATA-03):
- **Scenario A: Pure fabric order** -- Create supplier (use a real supplier name from 面料价格明细), create customer (use "Miraggo HomeLiving"), create fabric (use a real fabric from the file), create quote with 2 fabric items, convert quote to order, verify order has items
- **Scenario B: Mixed order (fabric + iron frame)** -- Create order directly with both fabric and product items from different categories
- **Scenario C: Quote-to-order conversion** -- Create quote with 3 items (2 fabric, 1 iron frame), partially convert (select 2 items), verify quote status is PARTIALLY_CONVERTED, convert remaining, verify CONVERTED

Each scenario includes:
- Exact field values to enter (real company names, real fabric names, real prices from the test files)
- Expected results after each step
- What to verify visually in the UI

Format as a checklist the user can follow step by step.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics && ls scripts/run-full-import-test.ts scripts/verify-page-stability.ts scripts/manual-entry-test-guide.md && node -e "const s1 = require('fs').readFileSync('scripts/run-full-import-test.ts','utf8'); const s2 = require('fs').readFileSync('scripts/verify-page-stability.ts','utf8'); if(!s1.includes('/api/v1/import') || !s2.includes('/api/v1/fabrics')) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run-full-import-test.ts exists and is >100 lines
    - run-full-import-test.ts contains all 13 xlsx file paths
    - run-full-import-test.ts contains two-stage import order (base data before orders)
    - run-full-import-test.ts contains summary table output
    - scripts/verify-page-stability.ts exists and is >60 lines
    - verify-page-stability.ts checks at least 6 list endpoints (/fabrics, /products, /suppliers, /customers, /orders, /quotes)
    - verify-page-stability.ts checks detail endpoints for first record of each type
    - verify-page-stability.ts outputs pass/fail summary
    - scripts/manual-entry-test-guide.md exists and is >40 lines
    - manual-entry-test-guide.md contains at least 3 scenarios (pure fabric, mixed, quote-to-order)
    - manual-entry-test-guide.md contains real data values from test files
  </acceptance_criteria>
  <done>Three deliverables created: (1) import test orchestrator that runs all 13 files in correct dependency order with result logging, (2) page stability checker that verifies all API endpoints return 200 with valid data, (3) manual entry guide with 3 real-data scenarios for user walkthrough</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute import tests and verify results in UI</name>
  <files>scripts/run-full-import-test.ts, scripts/verify-page-stability.ts</files>
  <action>
Claude executes before checkpoint:
1. Start the backend if not already running: `cd backend && pnpm start:dev`
2. Run data preparation: `cd scripts && npx ts-node prepare-fabric-pricelist.ts && npx ts-node prepare-product-pricelist.ts`
3. Run full import test: `cd scripts && npx ts-node run-full-import-test.ts`
4. Run page stability check: `cd scripts && npx ts-node verify-page-stability.ts`
5. Log all results for user review

Then pause for user manual verification:
- Open http://localhost:5173 in browser
- Navigate to each list page (面料, 产品, 供应商, 客户, 订单, 报价单) to confirm data appears
- Click into 2-3 detail pages of each type to confirm all fields populated
- Use search on list pages to confirm imported data is searchable
- Follow manual-entry-test-guide.md for at least Scenario A (pure fabric order)
- Check import page to confirm 4 tabs visible (面料, 供应商, 采购单, 购销合同)
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/scripts && npx ts-node verify-page-stability.ts</automated>
  </verify>
  <done>All 13 xlsx files imported successfully, page stability check passes (all list and detail endpoints return 200), user confirms data displays correctly in the UI</done>
</task>

</tasks>

<verification>
1. All 13 xlsx files processed through the import system (either via preparation scripts + existing strategies, or via new dedicated strategies)
2. Page stability check passes: all list and detail endpoints return 200
3. Manual entry test completed for at least one scenario
4. No 500 errors or broken pages after bulk import
5. Import results show data completeness (successCount matches expected row counts from source files)
</verification>

<success_criteria>
1. DATA-03: Manual entry test completed for supplier-customer-fabric-quote-order chain with real data
2. DATA-04: 面料价格明细2025.8.15.xlsx imported via preparation script + FabricImportStrategy
3. DATA-05: 铁架电机价格2025.xlsx imported via preparation script + ProductImportStrategy
4. DATA-06: 3x 海宁优途-采购单 imported via PurchaseOrderImportStrategy, 2x 购销合同 + 6x 客户订单 imported via SalesContractImportStrategy
5. DATA-07: All pages load correctly after bulk import -- verified by automated stability check + user manual sampling
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-data-testing/09-03-SUMMARY.md`
</output>
