---
phase: 09-real-data-testing
plan: 05
type: execute
wave: 2
depends_on: [09-04]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [DATA-03, DATA-06, DATA-07]

must_haves:
  truths:
    - "Sales contract import produces > 0 successful records from real 购销合同/客户订单 files"
    - "User has performed Scenario A from manual-entry-test-guide.md confirming the complete business chain works"
    - "REQUIREMENTS.md DATA-03, DATA-06, and DATA-07 checkboxes are updated to [x]"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes"
      contains: "[x] **DATA-03**"
  key_links:
    - from: "scripts/run-full-import-test.ts"
      to: "backend/src/import/strategies/sales-contract-import.strategy.ts"
      via: "HTTP import endpoint calls fixed strategy"
      pattern: "import/sales-contracts"
---

<objective>
Verify the SalesContractImportStrategy fixes from Plan 04 work against real files, have user perform the manual entry test (DATA-03), and update REQUIREMENTS.md checkboxes for DATA-03/DATA-06/DATA-07.

Purpose: Plan 04 fixed the code bugs. This plan validates those fixes produce real results, closes the DATA-03 manual test gap, and updates documentation traceability.

Output: Verified import results with > 0 successes from 购销合同/客户订单 files, user-confirmed manual entry test, updated REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-data-testing/09-04-SUMMARY.md
@scripts/manual-entry-test-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run sales contract import and verify > 0 successes</name>
  <read_first>
    - scripts/run-full-import-test.ts
    - .planning/phases/09-real-data-testing/09-04-SUMMARY.md
  </read_first>
  <files>
    (no files modified — validation only)
  </files>
  <action>
Re-run the sales contract and customer order import portion of the import test to verify Plan 04 fixes produce results.

1. Ensure backend is running at http://localhost:3000
2. Run the import test script focused on the 8 sales contract/customer order files:
   ```bash
   cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics
   npx ts-node --project scripts/tsconfig.json scripts/run-full-import-test.ts
   ```
3. Check output for Sales Contracts and Customer Orders rows — success count must be > 0
4. Record the actual success/skip/fail counts for the SUMMARY

Expected: Some data rows will still fail due to entity name mismatch (e.g., fabric "LEATHERGEL" not found in system) — this is acceptable per orchestrator decision. The key metric is > 0 successes (proving the column offset + customer resolution fixes work). Summary rows should now be counted as "skipped" not "failed".

If the test script requires a fresh database state (because previous import created orders), the executor should handle this by either:
- Using the existing data (if base entities still exist from prior import)
- Or re-running the full import test from stage 1 (fabrics -> products -> purchase orders -> sales contracts)
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/api/v1/orders?pageSize=5 | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Orders: {d.get(\"total\", d.get(\"meta\", {}).get(\"total\", 0))}'); sys.exit(0 if d.get('total', d.get('meta', {}).get('total', 0)) > 0 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Import script output shows Sales Contracts success > 0 OR Customer Orders success > 0 (at least some data rows parsed correctly)
    - Summary rows (合计, contract clauses) appear as "skipped" not "failed"
    - Customer resolution works (no "customerId is null" crashes)
    - Orders exist in the system after import: GET /api/v1/orders returns total > 0
  </acceptance_criteria>
  <done>
    - Sales contract import produces > 0 successful order records from real files (proving column fix works)
    - Entity name mismatches reported as clear failure messages (not crashes)
    - Summary/contract rows counted as skipped
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User performs manual entry test (DATA-03) and confirms page stability (DATA-07)</name>
  <files>(checkpoint — no files modified)</files>
  <action>
Present the user with verification instructions for DATA-03 and DATA-07. Wait for user confirmation.

Plan 04 fixed the SalesContractImportStrategy. Task 1 of this plan verified the fix produces real import results. The manual entry test guide exists at scripts/manual-entry-test-guide.md with 3 real-data scenarios.

**DATA-03 Manual Entry Test:**
1. Open http://localhost:5173
2. Follow Scenario A in scripts/manual-entry-test-guide.md:
   - Step 1: Verify supplier 博源纺织 exists (from prior import)
   - Step 2: Create a new customer (e.g., "Test Customer Co.")
   - Step 3: Create a fabric (or verify one exists from import)
   - Step 4: Create a quote with 2 items using real prices
   - Step 5: Convert quote to order
   - Step 6: Verify order detail shows correct items and prices
3. Confirm the complete supplier-customer-fabric-quote-order chain works end-to-end

**DATA-07 Page Stability:**
4. Navigate to each page after bulk import:
   - /products/fabrics (fabric list loads)
   - /products/iron-frame (iron frame list loads)
   - /suppliers (supplier list loads)
   - /customers (customer list loads)
   - /orders (order list loads, should include imported orders)
   - /quotes (quote list loads)
   - /import (import page shows 4 tabs)
5. Confirm no 500 errors, no broken layouts, no console errors

Resume signal: Type "approved" if both DATA-03 manual entry and DATA-07 page stability are confirmed, or describe any issues found.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user confirmation"</automated>
  </verify>
  <done>
    - User has confirmed DATA-03: complete supplier-customer-fabric-quote-order chain works with real data
    - User has confirmed DATA-07: all frontend pages load without errors after bulk import
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md checkboxes for DATA-03, DATA-06, DATA-07</name>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
Update the three requirement checkboxes in .planning/REQUIREMENTS.md:

1. Change line 79 from `- [ ] **DATA-03**: Manual entry test: create supplier, customer, fabric, quote, order with real data` to `- [x] **DATA-03**: Manual entry test: create supplier, customer, fabric, quote, order with real data`

2. Line 82 DATA-06 should already be `[x]` (verified in prior plans for 采购单 portion). If it is already checked, leave it. If unchecked, check it.

3. Change line 83 from `- [ ] **DATA-07**: System stability: all pages load correctly after bulk data import` to `- [x] **DATA-07**: System stability: all pages load correctly after bulk data import`

4. In the Traceability section at the bottom, update:
   - `DATA-03 | Phase 10 | Pending` to `DATA-03 | Phase 9 | Complete`
   - `DATA-07 | Phase 10 | Pending` to `DATA-07 | Phase 9 | Complete`

Note: The traceability section has incorrect phase numbers (Phase 10) — these should be Phase 9 since that is where the requirements are addressed. Fix these as well.
  </action>
  <verify>
    <automated>grep -c '\[x\] \*\*DATA-03\*\*\|\[x\] \*\*DATA-06\*\*\|\[x\] \*\*DATA-07\*\*' /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `[x] **DATA-03**`
    - REQUIREMENTS.md contains `[x] **DATA-06**`
    - REQUIREMENTS.md contains `[x] **DATA-07**`
    - Traceability section shows `DATA-03 | Phase 9 | Complete`
    - Traceability section shows `DATA-07 | Phase 9 | Complete`
  </acceptance_criteria>
  <done>
    - All three requirement checkboxes (DATA-03, DATA-06, DATA-07) are marked [x] in REQUIREMENTS.md
    - Traceability section reflects correct phase (9) and status (Complete)
    - Documentation matches actual verification state
  </done>
</task>

</tasks>

<verification>
1. Import script produces > 0 successes for sales contract/customer order files
2. User has confirmed DATA-03 manual entry test works
3. User has confirmed DATA-07 page stability
4. REQUIREMENTS.md DATA-03, DATA-06, DATA-07 all checked [x]
</verification>

<success_criteria>
- SalesContractImportStrategy successfully imports > 0 order records from real 购销合同 or 客户订单 files
- User has performed and approved Scenario A from manual-entry-test-guide.md
- All frontend pages load without errors after bulk import
- REQUIREMENTS.md checkboxes for DATA-03, DATA-06, DATA-07 are all [x]
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-data-testing/09-05-SUMMARY.md`
</output>
