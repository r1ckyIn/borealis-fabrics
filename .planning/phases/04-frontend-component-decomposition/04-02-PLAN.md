---
phase: 04-frontend-component-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useCustomerDetail.ts
  - frontend/src/pages/customers/CustomerDetailPage.tsx
  - frontend/src/pages/customers/components/CustomerBasicInfo.tsx
  - frontend/src/pages/customers/components/CustomerAddressTab.tsx
  - frontend/src/pages/customers/components/CustomerPricingTab.tsx
  - frontend/src/pages/customers/components/CustomerOrdersTab.tsx
  - frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx
  - frontend/src/hooks/__tests__/useCustomerDetail.test.ts
  - frontend/src/pages/customers/__tests__/CustomerBasicInfo.test.tsx
  - frontend/src/pages/customers/__tests__/CustomerAddressTab.test.tsx
  - frontend/src/pages/customers/__tests__/CustomerPricingTab.test.tsx
  - frontend/src/pages/customers/__tests__/CustomerOrdersTab.test.tsx
autonomous: true
requirements:
  - QUAL-04
  - QUAL-08
  - QUAL-09
  - TEST-07

must_haves:
  truths:
    - "CustomerDetailPage has zero useState calls — all state lives in useCustomerDetail hook"
    - "Each sub-component (CustomerBasicInfo, CustomerAddressTab, CustomerPricingTab, CustomerOrdersTab) has max 5 props"
    - "CustomerDetailPage still renders correctly with all tabs (info, addresses, pricing, orders)"
    - "All existing CustomerDetailPage tests continue passing (updated to mock custom hook)"
    - "Each sub-component has its own unit test file verifying rendering and prop handling"
  artifacts:
    - path: "frontend/src/hooks/useCustomerDetail.ts"
      provides: "Custom hook managing all CustomerDetailPage state + handlers"
      exports: ["useCustomerDetail"]
    - path: "frontend/src/pages/customers/components/CustomerBasicInfo.tsx"
      provides: "Basic info tab content (Descriptions display)"
      exports: ["CustomerBasicInfo"]
    - path: "frontend/src/pages/customers/components/CustomerAddressTab.tsx"
      provides: "Address management tab"
      exports: ["CustomerAddressTab"]
    - path: "frontend/src/pages/customers/components/CustomerPricingTab.tsx"
      provides: "Customer pricing tab with table + modal"
      exports: ["CustomerPricingTab"]
    - path: "frontend/src/pages/customers/components/CustomerOrdersTab.tsx"
      provides: "Customer order history tab"
      exports: ["CustomerOrdersTab"]
    - path: "frontend/src/pages/customers/CustomerDetailPage.tsx"
      provides: "Orchestrator page with zero useState"
      exports: ["default"]
    - path: "frontend/src/pages/customers/__tests__/CustomerBasicInfo.test.tsx"
      provides: "Unit tests for CustomerBasicInfo sub-component"
    - path: "frontend/src/pages/customers/__tests__/CustomerAddressTab.test.tsx"
      provides: "Unit tests for CustomerAddressTab sub-component"
    - path: "frontend/src/pages/customers/__tests__/CustomerPricingTab.test.tsx"
      provides: "Unit tests for CustomerPricingTab sub-component"
    - path: "frontend/src/pages/customers/__tests__/CustomerOrdersTab.test.tsx"
      provides: "Unit tests for CustomerOrdersTab sub-component"
  key_links:
    - from: "frontend/src/pages/customers/CustomerDetailPage.tsx"
      to: "frontend/src/hooks/useCustomerDetail.ts"
      via: "import { useCustomerDetail }"
      pattern: "useCustomerDetail"
    - from: "frontend/src/hooks/useCustomerDetail.ts"
      to: "@/hooks/queries/useCustomers"
      via: "composes existing query hooks"
      pattern: "useCustomer|useCustomerAddresses|useCustomerPricing|useCustomerOrders"
    - from: "frontend/src/pages/customers/CustomerDetailPage.tsx"
      to: "frontend/src/pages/customers/components/"
      via: "imports sub-components for tab rendering"
      pattern: "CustomerBasicInfo|CustomerAddressTab|CustomerPricingTab|CustomerOrdersTab"
---

<objective>
Decompose CustomerDetailPage (703 lines, 7 useState) into an orchestrator page + custom hook + 4 sub-components, mirroring the FabricDetailPage decomposition pattern. Includes unit tests for all sub-components.

Purpose: Reduce CustomerDetailPage complexity — all state moves to useCustomerDetail hook, rendering splits into focused sub-components with max 5 props each (QUAL-04, QUAL-08, QUAL-09).
Output: Refactored CustomerDetailPage.tsx (orchestrator), useCustomerDetail.ts (hook), 4 sub-components, updated tests, sub-component unit tests.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-component-decomposition/04-CONTEXT.md
@.planning/phases/04-frontend-component-decomposition/04-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From frontend/src/pages/orders/OrderDetailPage.tsx (REFERENCE PATTERN):
```typescript
export default function OrderDetailPage(): React.ReactElement {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const orderId = parseEntityId(id);
  const [activeTab, setActiveTab] = useState('items');
  // Single hook or minimal hooks, delegates to sub-components
}
```

From frontend/src/pages/orders/__tests__/OrderDetailPage.test.tsx (TEST MOCK PATTERN):
```typescript
vi.mock('../components/OrderInfoSection', () => ({
  OrderInfoSection: () => <div data-testid="order-info-section">Order Info</div>,
}));
```

From frontend/src/types/api.types.ts:
```typescript
export interface PaginatedResult<T> { items: T[]; pagination: PaginationMeta; }
export interface ApiError { code: number; message: string; data: null; }
```

From frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx (CURRENT MOCK PATTERN):
```typescript
// Currently mocks individual query hooks from useCustomers
// After decomposition: mock useCustomerDetail custom hook instead
```

From frontend/src/components/business/AddressManager.tsx:
```typescript
// Already an extracted component — CustomerAddressTab delegates to this
// Sub-component wraps AddressManager with data fetching and state
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract useCustomerDetail hook + create 4 sub-components + refactor CustomerDetailPage to orchestrator</name>
  <files>
    frontend/src/hooks/useCustomerDetail.ts,
    frontend/src/pages/customers/components/CustomerBasicInfo.tsx,
    frontend/src/pages/customers/components/CustomerAddressTab.tsx,
    frontend/src/pages/customers/components/CustomerPricingTab.tsx,
    frontend/src/pages/customers/components/CustomerOrdersTab.tsx,
    frontend/src/pages/customers/CustomerDetailPage.tsx
  </files>
  <read_first>
    frontend/src/pages/customers/CustomerDetailPage.tsx,
    frontend/src/pages/orders/OrderDetailPage.tsx,
    frontend/src/hooks/queries/useCustomers.ts,
    frontend/src/types/entities.types.ts,
    frontend/src/components/business/AddressManager.tsx,
    frontend/src/components/layout/PageContainer.tsx
  </read_first>
  <action>
    **Step 1: Create useCustomerDetail custom hook** (`frontend/src/hooks/useCustomerDetail.ts`)

    Extract ALL state from CustomerDetailPage.tsx into this hook:
    - Move all 7 `useState` calls (activeTab, deleteModalOpen, addressModal state, pricingModal state, and any others in the file)
    - Move all `Form.useForm()` calls
    - Move all query hook calls from `@/hooks/queries/useCustomers` (useCustomer, useCustomerOrders, useDeleteCustomer, and any customer pricing / address hooks)
    - Move all handler functions (handleDelete, address CRUD handlers, pricing CRUD handlers)
    - Move computed values (breadcrumbs)

    Hook signature: `export function useCustomerDetail(customerId: number | undefined)`

    Return type grouped by domain area:
    ```typescript
    return {
      data: { customer, isLoading, fetchError },
      tabs: { activeTab, setActiveTab },
      deleteCustomer: { modalOpen, setModalOpen, handle, isDeleting },
      addresses: { data, isLoading, modal, form, handlers... },
      pricing: { data, isLoading, modal, form, handlers... },
      orders: { data, isLoading },
      breadcrumbs,
    };
    ```

    **Step 2: Create sub-components** in `frontend/src/pages/customers/components/`

    Create directory `frontend/src/pages/customers/components/` if it doesn't exist.

    **CustomerBasicInfo.tsx** — Renders the "基本信息" tab content using Ant Design Descriptions. Props (max 5):
    ```typescript
    interface CustomerBasicInfoProps {
      customer: Customer;
      onEdit: () => void;
      onDelete: () => void;
      isDeleting: boolean;
    }
    ```
    Extract the Descriptions section showing companyName, contactName, phone, email, wechat, creditType, creditDays, billReceiveType, settleType, notes, dates.

    **CustomerAddressTab.tsx** — Renders the "地址管理" tab with address list. Props (max 5):
    ```typescript
    interface CustomerAddressTabProps {
      customerId: number;
      addresses: Address[] | undefined;
      isLoading: boolean;
      onAdd: (address: AddressData) => Promise<void>;
      onUpdate: (addressId: number, address: AddressData) => Promise<void>;
    }
    ```
    If the existing CustomerDetailPage delegates to `AddressManager` component, the sub-component should wrap AddressManager with the data-fetching context. Alternatively, if address management is inline, extract that inline code.

    **CustomerPricingTab.tsx** — Renders the "客户定价" tab with Table + add/edit Modal. Props (max 4, use ModalControl grouping):
    ```typescript
    interface CustomerPricingTabProps {
      pricing: PaginatedResult<CustomerPricing> | undefined;
      isLoading: boolean;
      modal: PricingModalControl;
      onDelete: (pricingId: number) => Promise<void>;
    }
    ```
    Extract the pricing Table, Add/Edit Modal with Form, and delete action.

    **CustomerOrdersTab.tsx** — Renders the "订单历史" tab with order list Table (read-only). Props (max 3):
    ```typescript
    interface CustomerOrdersTabProps {
      orders: PaginatedResult<Order> | undefined;
      isLoading: boolean;
      onViewOrder: (orderId: number) => void;
    }
    ```
    Extract the orders Table showing order history for this customer.

    **Step 3: Refactor CustomerDetailPage.tsx to orchestrator**

    Replace the 703-line component with an orchestrator that:
    1. Calls `useParams` and `useNavigate` (only router hooks remain in page)
    2. Calls `useCustomerDetail(customerId)` — single hook call
    3. Renders loading/error/not-found states
    4. Renders `<Tabs>` with 4 tab items, each delegating to a sub-component
    5. Has ZERO `useState` calls
    6. Keeps the default export and same file path (preserves lazy loading contract in routes/index.tsx)

    **Important:** All code comments must be in English only. No Chinese in code comments.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useState' frontend/src/pages/customers/CustomerDetailPage.tsx` returns 0
    - `grep 'useCustomerDetail' frontend/src/pages/customers/CustomerDetailPage.tsx` matches at least 1 line
    - `grep 'export function useCustomerDetail' frontend/src/hooks/useCustomerDetail.ts` matches
    - `grep 'export.*CustomerBasicInfo' frontend/src/pages/customers/components/CustomerBasicInfo.tsx` matches
    - `grep 'export.*CustomerAddressTab' frontend/src/pages/customers/components/CustomerAddressTab.tsx` matches
    - `grep 'export.*CustomerPricingTab' frontend/src/pages/customers/components/CustomerPricingTab.tsx` matches
    - `grep 'export.*CustomerOrdersTab' frontend/src/pages/customers/components/CustomerOrdersTab.tsx` matches
    - `grep 'export default function CustomerDetailPage' frontend/src/pages/customers/CustomerDetailPage.tsx` matches
    - `frontend/src/pages/customers/CustomerDetailPage.tsx` is under 150 lines (was 703)
    - Each sub-component Props interface has at most 5 required properties
    - `grep -rn 'as any\|: any\|<any>' frontend/src/hooks/useCustomerDetail.ts frontend/src/pages/customers/components/*.tsx` returns zero results (no accidental any types)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>CustomerDetailPage refactored to orchestrator with zero useState, useCustomerDetail hook extracts all state, 4 sub-components created with max 5 props each, no any types, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Update CustomerDetailPage tests to mock custom hook + sub-components</name>
  <files>
    frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx,
    frontend/src/hooks/__tests__/useCustomerDetail.test.ts
  </files>
  <read_first>
    frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx,
    frontend/src/pages/orders/__tests__/OrderDetailPage.test.tsx,
    frontend/src/hooks/useCustomerDetail.ts,
    frontend/src/pages/customers/CustomerDetailPage.tsx
  </read_first>
  <action>
    **Step 1: Update CustomerDetailPage.test.tsx**

    Replace the current individual hook mocks with:
    1. Mock useCustomerDetail custom hook (single mock replacing all individual hooks):
       ```typescript
       const mockUseCustomerDetail = vi.fn();
       vi.mock('@/hooks/useCustomerDetail', () => ({
         useCustomerDetail: (...args: unknown[]) => mockUseCustomerDetail(...args),
       }));
       ```
    2. Mock all 4 sub-components (following OrderDetailPage.test.tsx pattern):
       ```typescript
       vi.mock('../components/CustomerBasicInfo', () => ({
         CustomerBasicInfo: () => <div data-testid="customer-basic-info">Basic Info</div>,
       }));
       vi.mock('../components/CustomerAddressTab', () => ({
         CustomerAddressTab: () => <div data-testid="customer-address-tab">Addresses</div>,
       }));
       vi.mock('../components/CustomerPricingTab', () => ({
         CustomerPricingTab: () => <div data-testid="customer-pricing-tab">Pricing</div>,
       }));
       vi.mock('../components/CustomerOrdersTab', () => ({
         CustomerOrdersTab: () => <div data-testid="customer-orders-tab">Orders</div>,
       }));
       ```
    3. Update test cases to test orchestration logic only:
       - Loading state: mock returns `{ data: { isLoading: true } }` -> renders spinner
       - Error state: mock returns `{ data: { fetchError: new Error('fail') } }` -> renders error result
       - Not found state: mock returns `{ data: { customer: undefined, isLoading: false } }` -> renders 404
       - Success state: mock returns full data -> renders tabs with sub-component test IDs
       - Tab switching: verify activeTab/setActiveTab interaction
       - Delete: verify delete modal flow
       - Navigation: verify navigate('/customers') on back actions

    **Step 2: Create useCustomerDetail.test.ts** (`frontend/src/hooks/__tests__/useCustomerDetail.test.ts`)

    Test the custom hook using `renderHook` from `@testing-library/react`:
    1. Mock all individual query hooks from `@/hooks/queries/useCustomers`
    2. Test initial state: activeTab is 'info', modals closed, no errors
    3. Test tab switching: setActiveTab updates activeTab
    4. Test address/pricing modal open/close
    5. Test that hook calls useCustomer(customerId) with correct argument
    6. Test error propagation

    Use `@tanstack/react-query` QueryClientProvider wrapper for renderHook.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm test -- --testPathPattern="CustomerDetailPage|useCustomerDetail" --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'useCustomerDetail' frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx` matches (mocks custom hook)
    - `grep 'customer-basic-info' frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx` matches (mocks sub-component)
    - `grep 'customer-address-tab' frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx` matches
    - File `frontend/src/hooks/__tests__/useCustomerDetail.test.ts` exists
    - `grep 'renderHook' frontend/src/hooks/__tests__/useCustomerDetail.test.ts` matches
    - `pnpm test -- --testPathPattern="CustomerDetailPage|useCustomerDetail"` exits 0 with all tests passing
  </acceptance_criteria>
  <done>CustomerDetailPage test updated to mock custom hook + sub-components, useCustomerDetail hook has unit tests, all customer-related tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for 4 customer sub-components</name>
  <files>
    frontend/src/pages/customers/__tests__/CustomerBasicInfo.test.tsx,
    frontend/src/pages/customers/__tests__/CustomerAddressTab.test.tsx,
    frontend/src/pages/customers/__tests__/CustomerPricingTab.test.tsx,
    frontend/src/pages/customers/__tests__/CustomerOrdersTab.test.tsx
  </files>
  <read_first>
    frontend/src/pages/customers/components/CustomerBasicInfo.tsx,
    frontend/src/pages/customers/components/CustomerAddressTab.tsx,
    frontend/src/pages/customers/components/CustomerPricingTab.tsx,
    frontend/src/pages/customers/components/CustomerOrdersTab.tsx,
    frontend/src/types/entities.types.ts
  </read_first>
  <action>
    Create unit test files for each of the 4 customer sub-components created in Task 1. Each test file should follow existing test patterns (QueryClientProvider wrapper, vi.mock for dependencies).

    **CustomerBasicInfo.test.tsx:**
    - Test renders customer fields (companyName, contactName, phone, email, etc.) using Ant Design Descriptions
    - Test onEdit callback fires when edit button clicked
    - Test onDelete callback fires when delete button clicked
    - Test isDeleting prop disables delete button

    **CustomerAddressTab.test.tsx:**
    - Test renders address list when addresses provided
    - Test renders empty state when addresses is undefined/empty
    - Test delegates to AddressManager component (if applicable)
    - Test onAdd callback is passed through correctly

    **CustomerPricingTab.test.tsx:**
    - Test renders pricing table with data
    - Test renders empty table when no pricing data
    - Test modal.onOpen fires when add button clicked
    - Test onDelete callback fires on delete action

    **CustomerOrdersTab.test.tsx:**
    - Test renders orders table with data
    - Test renders empty table when no orders
    - Test onViewOrder callback fires when row action clicked

    Each test file should have 3-5 focused test cases. Use proper types throughout.

    **Important:** No `as any`, `: any`, or `<any>` in any test file.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm test -- --testPathPattern="CustomerBasicInfo|CustomerAddressTab|CustomerPricingTab|CustomerOrdersTab" --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/pages/customers/__tests__/CustomerBasicInfo.test.tsx` exists with 3+ test cases
    - File `frontend/src/pages/customers/__tests__/CustomerAddressTab.test.tsx` exists with 3+ test cases
    - File `frontend/src/pages/customers/__tests__/CustomerPricingTab.test.tsx` exists with 3+ test cases
    - File `frontend/src/pages/customers/__tests__/CustomerOrdersTab.test.tsx` exists with 3+ test cases
    - `grep -rn 'as any\|: any\|<any>' frontend/src/pages/customers/__tests__/CustomerBasicInfo.test.tsx frontend/src/pages/customers/__tests__/CustomerAddressTab.test.tsx frontend/src/pages/customers/__tests__/CustomerPricingTab.test.tsx frontend/src/pages/customers/__tests__/CustomerOrdersTab.test.tsx` returns zero results
    - `pnpm test -- --testPathPattern="CustomerBasicInfo|CustomerAddressTab|CustomerPricingTab|CustomerOrdersTab"` exits 0
  </acceptance_criteria>
  <done>All 4 customer sub-components have unit tests covering rendering, prop callbacks, and edge cases. No any types. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -c 'useState' frontend/src/pages/customers/CustomerDetailPage.tsx` returns 0 (QUAL-09)
2. All sub-component Props interfaces have at most 5 properties (QUAL-08)
3. `cd frontend && pnpm test -- --testPathPattern="CustomerDetailPage|useCustomerDetail|CustomerBasicInfo|CustomerAddressTab|CustomerPricingTab|CustomerOrdersTab" --reporter=verbose` passes (TEST-07)
4. `cd frontend && pnpm build` passes (no type errors)
5. `cd frontend && pnpm typecheck` passes
6. `grep -rn 'as any\|: any\|<any>' frontend/src/hooks/useCustomerDetail.ts frontend/src/pages/customers/components/*.tsx` returns zero results
</verification>

<success_criteria>
- CustomerDetailPage reduced from 703 lines to under 150 lines (orchestrator only)
- useCustomerDetail hook contains all state + handlers
- 4 sub-components (CustomerBasicInfo, CustomerAddressTab, CustomerPricingTab, CustomerOrdersTab) each with max 5 props
- Zero useState in CustomerDetailPage.tsx
- All existing customer tests continue passing with updated mocks
- New useCustomerDetail.test.ts hook tests pass
- All 4 sub-component test files created and passing
- Zero any types in new files
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-component-decomposition/04-02-SUMMARY.md`
</output>
