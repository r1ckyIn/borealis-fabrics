---
phase: 01-frontend-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - frontend/src/pages/fabrics/FabricListPage.tsx
  - frontend/src/pages/fabrics/FabricFormPage.tsx
  - frontend/src/pages/fabrics/FabricDetailPage.tsx
  - frontend/src/pages/fabrics/__tests__/FabricListPage.test.tsx
  - frontend/src/pages/fabrics/__tests__/FabricFormPage.test.tsx
  - frontend/src/pages/fabrics/__tests__/FabricDetailPage.test.tsx
  - frontend/src/pages/quotes/QuoteListPage.tsx
  - frontend/src/pages/quotes/QuoteFormPage.tsx
  - frontend/src/pages/quotes/QuoteDetailPage.tsx
  - frontend/src/api/fabric.api.ts
  - frontend/src/api/quote.api.ts
  - frontend/src/hooks/queries/useFabrics.ts
  - frontend/src/hooks/queries/useQuotes.ts
  - backend/src/fabric/dto/query-fabric.dto.ts
  - backend/src/quote/dto/query-quote.dto.ts
autonomous: true
requirements: [BUGF-01, BUGF-02, BUGF-03, BUGF-04, BUGF-05, BUGF-06]
must_haves:
  truths:
    - "Fabric list page search filters by fabricCode/name/color and results update correctly"
    - "Quote list page search works with correct parameters matching backend DTO"
    - "Delete buttons on fabric and quote list pages show specific Chinese error messages"
    - "Quote convert-to-order button handles 501 Not Implemented gracefully with Chinese message"
    - "Fabric and quote list pages show Empty component with action button when no data"
    - "All async operations on fabric and quote pages show loading states"
  artifacts:
    - path: "frontend/src/pages/fabrics/FabricListPage.tsx"
      provides: "Fixed fabric list with correct search params, error handling, empty state, loading state"
      contains: "getErrorMessage"
    - path: "frontend/src/pages/quotes/QuoteListPage.tsx"
      provides: "Fixed quote list with error handling, empty state, loading state"
      contains: "getErrorMessage"
    - path: "frontend/src/pages/quotes/QuoteDetailPage.tsx"
      provides: "Fixed quote detail with graceful 501 handling for convert-to-order"
      contains: "getErrorMessage"
  key_links:
    - from: "frontend/src/pages/fabrics/FabricListPage.tsx"
      to: "frontend/src/utils/errorMessages.ts"
      via: "imports getErrorMessage"
      pattern: "import.*getErrorMessage.*errorMessages"
    - from: "frontend/src/pages/quotes/QuoteDetailPage.tsx"
      to: "frontend/src/utils/errorMessages.ts"
      via: "imports getErrorMessage for 501 handling"
      pattern: "import.*getErrorMessage.*errorMessages"
    - from: "frontend/src/api/fabric.api.ts"
      to: "backend/src/fabric/fabric.controller.ts"
      via: "HTTP GET /api/v1/fabrics with fabricCode/name/color params"
      pattern: "fabricCode|name|color"
---

<objective>
Fix all bugs in Fabric and Quote modules. Fabric has a complex search mismatch (keyword vs fabricCode/name/color). Quote has the convert-to-order 501 handling issue plus standard error/loading/empty state bugs.

Purpose: Fabric is the core product entity; Quote is the first business-flow entity. Fixing these completes coverage of the "data entry" portion of the business flow (Supplier -> Customer -> Fabric -> Quote).

Output: Working Fabric + Quote pages with correct API alignment, Chinese error messages, graceful 501 handling, loading states, empty states, and regression tests.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-bug-fixes/01-CONTEXT.md
@.planning/phases/01-frontend-bug-fixes/01-RESEARCH.md
@.planning/phases/01-frontend-bug-fixes/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/utils/errorMessages.ts (created in Plan 01):
```typescript
export const ERROR_CODE_MESSAGES: Record<string, string>;
export const HTTP_STATUS_MESSAGES: Record<number, string>;
export function getErrorMessage(error: ApiError): string;
export function getDeleteErrorMessage(error: ApiError, entityName: string): string;
```

From frontend/src/types/api.types.ts:
```typescript
export interface ApiError {
  code: number;
  message: string;
  data: null;
}
```

Expected backend DTO fields:
- QueryFabricDto: fabricCode, name, color, supplierId, page, pageSize (NOT keyword)
- QueryQuoteDto: quoteCode, customerId, status, page, pageSize (NOT keyword)

Known P1 bugs from research:
- Fabric search sends `keyword` + `supplierId` but backend expects `fabricCode` / `name` / `color`
- Quote convert-to-order returns 501 Not Implemented (Phase 2 scope to implement, Phase 1 must handle gracefully)
- Error handling uses `error.response?.status` (wrong) in QuoteListPage and QuoteDetailPage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Fabric Module (List + Form + Detail Pages)</name>
  <files>
    frontend/src/pages/fabrics/FabricListPage.tsx
    frontend/src/pages/fabrics/FabricFormPage.tsx
    frontend/src/pages/fabrics/FabricDetailPage.tsx
    frontend/src/api/fabric.api.ts
    frontend/src/hooks/queries/useFabrics.ts
    frontend/src/pages/fabrics/__tests__/FabricListPage.test.tsx
    frontend/src/pages/fabrics/__tests__/FabricFormPage.test.tsx
    frontend/src/pages/fabrics/__tests__/FabricDetailPage.test.tsx
    backend/src/fabric/dto/query-fabric.dto.ts
  </files>
  <read_first>
    .planning/phases/01-frontend-bug-fixes/01-AUDIT.md
    frontend/src/pages/fabrics/FabricListPage.tsx
    frontend/src/pages/fabrics/FabricFormPage.tsx
    frontend/src/pages/fabrics/FabricDetailPage.tsx
    frontend/src/api/fabric.api.ts
    frontend/src/hooks/queries/useFabrics.ts
    frontend/src/types/api.types.ts
    frontend/src/types/entities.types.ts
    frontend/src/utils/errorMessages.ts
    frontend/src/pages/fabrics/__tests__/FabricListPage.test.tsx
    frontend/src/pages/fabrics/__tests__/FabricFormPage.test.tsx
    frontend/src/pages/fabrics/__tests__/FabricDetailPage.test.tsx
    backend/src/fabric/fabric.controller.ts
    backend/src/fabric/dto/query-fabric.dto.ts
    backend/src/fabric/fabric.service.ts
  </read_first>
  <action>
    Consult 01-AUDIT.md for all Fabric module bugs. Apply ALL fixes. At minimum:

    **1. Search Parameter Fix (FabricListPage.tsx) -- This is the most complex search fix:**
    - Fabric backend `QueryFabricDto` expects `fabricCode`, `name`, `color`, `supplierId` -- NOT `keyword`
    - Decision (per audit): either (a) add `keyword` field to `QueryFabricDto` that searches across fabricCode/name/color using OR logic in the backend service, or (b) change frontend to show separate search fields
    - Recommended approach (a): Add `@IsOptional() @IsString() keyword?: string` to `QueryFabricDto`, then in `FabricService.findAll()` add a `keyword` WHERE clause that does `OR(fabricCode CONTAINS keyword, name CONTAINS keyword, color CONTAINS keyword)`. This provides the unified search UX the frontend expects
    - Update `fabric.api.ts` to pass the correct parameter names
    - Verify via `useFabrics.ts` query hook that params flow correctly

    **2. Error Handling Fix (all Fabric pages):**
    - Replace ALL `error.response?.status` with `error.code`
    - Import `getErrorMessage`, `getDeleteErrorMessage` from `@/utils/errorMessages`
    - FabricListPage delete: `getDeleteErrorMessage(error, '面料')`
    - FabricFormPage submit: `message.error(getErrorMessage(error as ApiError))`
    - FabricDetailPage: error state display with `getErrorMessage()`

    **3. Loading States:**
    - FabricListPage: `loading={isLoading}` on Table, `loading` on delete button
    - FabricFormPage: `loading` on submit button during mutation
    - FabricDetailPage: `LoadingSpinner` while data loads (769 lines -- DO NOT refactor, Phase 4 scope)

    **4. Empty States (FabricListPage.tsx):**
    - `locale={{ emptyText: <Empty description="暂无面料数据" image={Empty.PRESENTED_IMAGE_SIMPLE}><Button type="primary" icon={<PlusOutlined />} onClick={() => navigate('/fabrics/new')}>新建面料</Button></Empty> }}`

    **5. Detail Page (FabricDetailPage.tsx):**
    - 769 lines -- only fix bugs, DO NOT decompose (Phase 4 QUAL-03)
    - Ensure error/loading states work correctly
    - Ensure all backend fields are displayed

    **6. Update Tests:**
    - FabricListPage.test.tsx: search param, empty state, delete error tests
    - FabricFormPage.test.tsx: submit error message test
    - FabricDetailPage.test.tsx: loading/error state tests

    **7. Backend DTO/Service Fix (if keyword approach chosen):**
    - Add `keyword` to `QueryFabricDto`
    - Update `FabricService.findAll()` WHERE clause
    - Update backend tests for new search logic

    Run: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck`
    If backend changed: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm build && pnpm test && pnpm lint`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/fabrics/FabricListPage.tsx` returns >= 1
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/fabrics/FabricFormPage.tsx` returns >= 1
    - `grep "error\.response\?\.status" frontend/src/pages/fabrics/FabricListPage.tsx` returns NO matches (exit code 1)
    - `grep -c "Empty" frontend/src/pages/fabrics/FabricListPage.tsx` returns >= 1
    - `grep -c "loading" frontend/src/pages/fabrics/FabricListPage.tsx` returns >= 1
    - `cd frontend && pnpm test -- --run src/pages/fabrics/__tests__/` exits 0
    - `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Fabric module fully fixed: search works correctly (by fabricCode/name/color or keyword), delete shows Chinese errors, form shows specific errors, empty state with action button, loading states on all async ops. All fabric tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Quote Module (List + Form + Detail Pages)</name>
  <files>
    frontend/src/pages/quotes/QuoteListPage.tsx
    frontend/src/pages/quotes/QuoteFormPage.tsx
    frontend/src/pages/quotes/QuoteDetailPage.tsx
    frontend/src/api/quote.api.ts
    frontend/src/hooks/queries/useQuotes.ts
    backend/src/quote/dto/query-quote.dto.ts
  </files>
  <read_first>
    .planning/phases/01-frontend-bug-fixes/01-AUDIT.md
    frontend/src/pages/quotes/QuoteListPage.tsx
    frontend/src/pages/quotes/QuoteFormPage.tsx
    frontend/src/pages/quotes/QuoteDetailPage.tsx
    frontend/src/api/quote.api.ts
    frontend/src/hooks/queries/useQuotes.ts
    frontend/src/types/api.types.ts
    frontend/src/utils/errorMessages.ts
    backend/src/quote/quote.controller.ts
    backend/src/quote/dto/query-quote.dto.ts
    backend/src/quote/quote.service.ts
  </read_first>
  <action>
    Consult 01-AUDIT.md for all Quote module bugs. Apply ALL fixes. Key areas:

    **1. Convert-to-Order 501 Handling (QuoteDetailPage.tsx) -- CRITICAL:**
    - The convert-to-order endpoint returns 501 Not Implemented (Phase 2 will implement it)
    - Frontend MUST handle this gracefully: catch the error, check `error.code === 501`, display `message.warning('该功能暂未实现，请等待后续更新')` (NOT an error toast, use warning)
    - The `HTTP_STATUS_MESSAGES` from errorMessages.ts already maps 501 -> '该功能暂未实现'
    - Ensure the convert button exists but shows a user-friendly message when clicked

    **2. Search Parameter Fix (QuoteListPage.tsx):**
    - Verify frontend search params match `QueryQuoteDto` fields (quoteCode, customerId, status)
    - If frontend sends `keyword`, add keyword support to backend DTO or change frontend to use specific fields
    - Ensure status filter works correctly with the QuoteStatus enum values

    **3. Error Handling Fix (all Quote pages):**
    - Replace ALL `error.response?.status` with `error.code`
    - Import `getErrorMessage`, `getDeleteErrorMessage` from `@/utils/errorMessages`
    - QuoteListPage delete: `getDeleteErrorMessage(error, '报价单')`
    - QuoteFormPage submit: `message.error(getErrorMessage(error as ApiError))`

    **4. Loading States:**
    - QuoteListPage: `loading={isLoading}` on Table
    - QuoteFormPage: `loading` on submit button
    - QuoteDetailPage: `LoadingSpinner` while loading, `loading` on convert button during mutation

    **5. Empty States (QuoteListPage.tsx):**
    - `locale={{ emptyText: <Empty description="暂无报价单数据" image={Empty.PRESENTED_IMAGE_SIMPLE}><Button type="primary" icon={<PlusOutlined />} onClick={() => navigate('/quotes/new')}>新建报价单</Button></Empty> }}`

    **6. Create Tests (Quote pages have NO existing tests):**
    - Create `frontend/src/pages/quotes/__tests__/QuoteListPage.test.tsx`:
      - Test: renders table with data
      - Test: shows empty state when no quotes
      - Test: delete error shows Chinese message
      - Test: search form submits with correct parameters
    - Create `frontend/src/pages/quotes/__tests__/QuoteDetailPage.test.tsx`:
      - Test: renders quote details
      - Test: convert-to-order button shows warning on 501
      - Test: loading state while data loads
    - Create `frontend/src/pages/quotes/__tests__/QuoteFormPage.test.tsx`:
      - Test: renders form
      - Test: submit error shows Chinese message
    - Follow existing test patterns from supplier/customer `__tests__/` directories

    **7. Backend DTO Fix (if needed per audit):**
    - Same approach as other modules -- follow audit direction

    Run: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck`
    If backend changed: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm build && pnpm test && pnpm lint`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/quotes/QuoteListPage.tsx` returns >= 1
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/quotes/QuoteDetailPage.tsx` returns >= 1
    - `grep "error\.response\?\.status" frontend/src/pages/quotes/QuoteListPage.tsx` returns NO matches (exit code 1)
    - `grep "error\.response\?\.status" frontend/src/pages/quotes/QuoteDetailPage.tsx` returns NO matches (exit code 1)
    - `grep -c "501\|暂未实现" frontend/src/pages/quotes/QuoteDetailPage.tsx` returns >= 1 (501 handling)
    - `grep -c "Empty" frontend/src/pages/quotes/QuoteListPage.tsx` returns >= 1
    - Quote test directory exists: `test -d frontend/src/pages/quotes/__tests__`
    - At least 3 quote test files exist: `ls frontend/src/pages/quotes/__tests__/*.test.tsx | wc -l` returns >= 3
    - `cd frontend && pnpm test -- --run src/pages/quotes/__tests__/` exits 0
    - `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Quote module fully fixed: convert-to-order shows friendly 501 warning, search works correctly, delete shows Chinese errors, form shows specific errors, empty state with action button, loading states. New test suite created for all 3 quote pages. All tests pass.</done>
</task>

</tasks>

<verification>
1. Fabric and Quote modules use `getErrorMessage`/`getDeleteErrorMessage` from errorMessages utility
2. No remaining `error.response?.status` patterns in any Fabric or Quote page
3. Quote convert-to-order gracefully handles 501 with Chinese warning message
4. Fabric search works with correct backend DTO params
5. All list pages have Empty state and loading states
6. Quote pages have new test suite (previously had no tests)
7. Full suite: `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck` passes
8. If backend changed: `cd backend && pnpm build && pnpm test && pnpm lint` passes
</verification>

<success_criteria>
- Fabric search filters work correctly
- Quote convert-to-order shows user-friendly 501 message
- All error handling uses ApiError pattern with Chinese messages
- Empty states and loading states on all pages
- New quote test suite exists with >= 3 test files
- No regressions in any existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-bug-fixes/01-03-SUMMARY.md`
</output>
