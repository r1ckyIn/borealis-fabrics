---
phase: 16-production-deployment
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-13T08:14:41.366Z
---

<current_state>
Phase 16 execute-phase, Wave 2, Plan 16-03, Task 3 (checkpoint:human-verify).
Plans 16-01 and 16-02 are complete. All cloud resources provisioned and verified.
Ready to deploy the application to the server.
</current_state>

<completed_work>

**Plans 16-01 & 16-02 (Wave 1) — COMPLETE:**
- backend/Dockerfile — multi-stage build with Prisma, non-root user, HEALTHCHECK
- backend/.dockerignore — build context exclusions
- nginx/nginx.conf + nginx/conf.d/default.conf — reverse proxy, SPA routing, gzip, static cache
- docker-compose.prod.yml — NestJS (768M) + Redis (192M) + Nginx (64M)
- backend/src/auth/auth.controller.ts — ALLOW_DEV_LOGIN + FORCE_HTTPS_COOKIES env vars
- backend/src/config/configuration.ts — conditional WEWORK validation
- backend/.env.production.example — complete env template

**Plan 16-03 Tasks 1-2 — COMPLETE:**
- deploy/deploy.sh — automated deployment script
- deploy/rollback.sh — rollback to specific commit
- docs/DEPLOY.md — bilingual deployment guide (Phase A + B + UAT checklist)

**Cloud Provisioning — COMPLETE:**
- Lighthouse server: 119.29.82.146 (Ubuntu 22.04, Docker 26.1.3, 2C4G, Guangzhou)
- Lighthouse DB: 10.1.0.9:3306 (MySQL 5.7, borealis database created)
- COS: borealis-files-1421178041 (ap-guangzhou, CORS configured)
- WeChat Work: app created (CorpID, AgentId, Secret obtained)
- COS API keys: SecretId + SecretKey obtained
- SSH key auth: configured and verified
- Firewall: ports 22, 80, 443, 3000 open
</completed_work>

<remaining_work>

- Task 3: Deploy application to server (Phase A — IP + HTTP + dev login)
  1. Clone repo on server
  2. Create backend/.env with all credentials (see memory/project_server_access.md)
  3. Build frontend locally or on server
  4. Run docker compose -f docker-compose.prod.yml up -d
  5. Run prisma migrate deploy
  6. Verify health endpoint + frontend loads + dev login works
  7. Run UAT checklist
- After Task 3: Phase verification + ROADMAP update + phase completion
</remaining_work>

<decisions_made>

- Lighthouse + Lighthouse DB (not CVM + CDB) — same VPC ecosystem, no CCN needed
- All resources in Guangzhou (DB was created there first)
- MySQL 5.7 (not 8.0) — Lighthouse DB promo only had 5.7, Prisma supports both
- ALLOW_DEV_LOGIN=true for Phase A — no domain yet, WeChat OAuth requires domain
- FORCE_HTTPS_COOKIES=false for Phase A — HTTP only, secure cookies would break login
- Monthly billing for trial — user wants to test 1 month before committing to annual
</decisions_made>

<blockers>
None — all prerequisites complete, ready to deploy.
</blockers>

<context>
All credentials stored in memory/project_server_access.md (server IP, DB password, COS keys, WeChat Work credentials). User authorized Claude to SSH directly to server without asking permission. The GSD execute-phase workflow is at the checkpoint task — after deployment and UAT pass, continue with phase verification and completion.

Key codebase note: docker-compose.prod.yml references external MySQL (not in compose), Redis runs in container, Nginx serves frontend static files and proxies /api/ to NestJS. The monitoring stack (Grafana/Loki/Prometheus) from docker-compose.monitoring.yml should be deployed after business stack is verified stable.
</context>

<next_action>
Start with: Read memory/project_server_access.md for all credentials, then SSH to 119.29.82.146, clone repo, create .env, build frontend, docker compose up, verify health + login. This is the Phase A deployment.
</next_action>
