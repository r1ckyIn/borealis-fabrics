---
phase: 01-frontend-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - frontend/src/pages/suppliers/SupplierListPage.tsx
  - frontend/src/pages/suppliers/SupplierFormPage.tsx
  - frontend/src/pages/suppliers/SupplierDetailPage.tsx
  - frontend/src/pages/suppliers/__tests__/SupplierListPage.test.tsx
  - frontend/src/pages/suppliers/__tests__/SupplierFormPage.test.tsx
  - frontend/src/pages/suppliers/__tests__/SupplierDetailPage.test.tsx
  - frontend/src/pages/customers/CustomerListPage.tsx
  - frontend/src/pages/customers/CustomerFormPage.tsx
  - frontend/src/pages/customers/CustomerDetailPage.tsx
  - frontend/src/pages/customers/__tests__/CustomerListPage.test.tsx
  - frontend/src/pages/customers/__tests__/CustomerFormPage.test.tsx
  - frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx
  - frontend/src/hooks/queries/useSuppliers.ts
  - frontend/src/hooks/queries/useCustomers.ts
  - frontend/src/api/supplier.api.ts
  - frontend/src/api/customer.api.ts
  - backend/src/supplier/dto/query-supplier.dto.ts
  - backend/src/customer/dto/query-customer.dto.ts
autonomous: true
requirements: [BUGF-01, BUGF-02, BUGF-03, BUGF-04, BUGF-05, BUGF-06]
must_haves:
  truths:
    - "Supplier list page search filters by companyName and results update correctly"
    - "Customer list page search filters by companyName and results update correctly"
    - "Delete buttons on supplier and customer list pages show specific Chinese error messages on failure (e.g., 409 conflict)"
    - "Supplier and customer form pages show Chinese error messages from backend on submit failure"
    - "Supplier and customer form pages display field-level validation errors inline on the corresponding form field when backend returns 400/422 with field-specific errors"
    - "Supplier and customer list pages show Ant Design Empty component with action button when no data"
    - "All async operations on supplier and customer pages show loading states"
  artifacts:
    - path: "frontend/src/pages/suppliers/SupplierListPage.tsx"
      provides: "Fixed supplier list with correct search params, error handling, empty state, loading state"
      contains: "getErrorMessage"
    - path: "frontend/src/pages/customers/CustomerListPage.tsx"
      provides: "Fixed customer list with correct search params, error handling, empty state, loading state"
      contains: "getErrorMessage"
    - path: "frontend/src/pages/suppliers/__tests__/SupplierListPage.test.tsx"
      provides: "Updated tests covering error handling, empty state, loading state"
      contains: "getErrorMessage"
    - path: "frontend/src/pages/customers/__tests__/CustomerListPage.test.tsx"
      provides: "Updated tests covering error handling, empty state, loading state"
      contains: "getErrorMessage"
  key_links:
    - from: "frontend/src/pages/suppliers/SupplierListPage.tsx"
      to: "frontend/src/utils/errorMessages.ts"
      via: "imports getErrorMessage"
      pattern: "import.*getErrorMessage.*errorMessages"
    - from: "frontend/src/pages/customers/CustomerListPage.tsx"
      to: "frontend/src/utils/errorMessages.ts"
      via: "imports getErrorMessage"
      pattern: "import.*getErrorMessage.*errorMessages"
    - from: "frontend/src/api/supplier.api.ts"
      to: "backend/src/supplier/supplier.controller.ts"
      via: "HTTP GET /api/v1/suppliers with companyName param"
      pattern: "companyName"
---

<objective>
Fix all bugs in Supplier and Customer modules -- the two simplest CRUD modules that share identical patterns. This establishes the fix template for all remaining modules.

Purpose: Supplier and Customer are the foundation entities in the business flow. Fixing them first (per locked decision: business flow order) establishes the error handling, search, loading, and empty state patterns that Plans 03-04 replicate.

Output: Working Supplier + Customer pages with correct API alignment, Chinese error messages, loading states, empty states, and updated regression tests.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-bug-fixes/01-CONTEXT.md
@.planning/phases/01-frontend-bug-fixes/01-RESEARCH.md
@.planning/phases/01-frontend-bug-fixes/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/types/api.types.ts:
```typescript
export interface ApiError {
  code: number;
  message: string;
  data: null;
}
```

From frontend/src/utils/errorMessages.ts (created in Plan 01):
```typescript
export const ERROR_CODE_MESSAGES: Record<string, string>;
export const HTTP_STATUS_MESSAGES: Record<number, string>;
export function getErrorMessage(error: ApiError): string;
export function getDeleteErrorMessage(error: ApiError, entityName: string): string;
```

From frontend/src/api/client.ts (error interceptor):
```typescript
// Rejects with ApiError, NOT AxiosError
const apiError: ApiError = {
  code: error.response?.data?.code ?? error.response?.status ?? 500,
  message: error.response?.data?.message ?? error.message ?? 'Unknown error',
  data: null,
};
return Promise.reject(apiError);
```

Expected backend DTO fields:
- QuerySupplierDto: companyName, contactPerson, page, pageSize (NOT keyword)
- QueryCustomerDto: companyName, contactPerson, page, pageSize (NOT keyword)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Supplier Module (List + Form + Detail Pages)</name>
  <files>
    frontend/src/pages/suppliers/SupplierListPage.tsx
    frontend/src/pages/suppliers/SupplierFormPage.tsx
    frontend/src/pages/suppliers/SupplierDetailPage.tsx
    frontend/src/api/supplier.api.ts
    frontend/src/hooks/queries/useSuppliers.ts
    frontend/src/pages/suppliers/__tests__/SupplierListPage.test.tsx
    frontend/src/pages/suppliers/__tests__/SupplierFormPage.test.tsx
    frontend/src/pages/suppliers/__tests__/SupplierDetailPage.test.tsx
    backend/src/supplier/dto/query-supplier.dto.ts
  </files>
  <read_first>
    .planning/phases/01-frontend-bug-fixes/01-AUDIT.md
    frontend/src/pages/suppliers/SupplierListPage.tsx
    frontend/src/pages/suppliers/SupplierFormPage.tsx
    frontend/src/pages/suppliers/SupplierDetailPage.tsx
    frontend/src/api/supplier.api.ts
    frontend/src/hooks/queries/useSuppliers.ts
    frontend/src/types/api.types.ts
    frontend/src/types/entities.types.ts
    frontend/src/utils/errorMessages.ts
    frontend/src/api/client.ts
    frontend/src/pages/suppliers/__tests__/SupplierListPage.test.tsx
    frontend/src/pages/suppliers/__tests__/SupplierFormPage.test.tsx
    frontend/src/pages/suppliers/__tests__/SupplierDetailPage.test.tsx
    backend/src/supplier/supplier.controller.ts
    backend/src/supplier/dto/query-supplier.dto.ts
    backend/src/supplier/supplier.service.ts
  </read_first>
  <action>
    Consult the AUDIT report (01-AUDIT.md) for the complete bug list for the Supplier module. Apply ALL fixes identified in the audit for this module. At minimum, the following systematic fixes are required:

    **1. Search Parameter Fix (SupplierListPage.tsx):**
    - Change frontend search form to send `companyName` parameter instead of `keyword`
    - Update the `SearchForm` field configuration to use `companyName` as the field name
    - If backend `QuerySupplierDto` supports `contactPerson`, add that as an optional search field too
    - Verify the API layer (`supplier.api.ts`) passes search params correctly to `GET /api/v1/suppliers`

    **2. Error Handling Fix (SupplierListPage.tsx):**
    - Replace ALL instances of `error.response?.status` with `error.code` (ApiError pattern)
    - Import `getErrorMessage` and `getDeleteErrorMessage` from `@/utils/errorMessages`
    - Delete handler: use `getDeleteErrorMessage(error, '供应商')` for the error message
    - Form submit errors: use `getErrorMessage(error)` for toast notifications

    **3. Error Handling Fix (SupplierFormPage.tsx) -- including inline field validation (per user decision):**
    - Replace error catch blocks to use `ApiError` type and `getErrorMessage()`
    - **CRITICAL (locked decision):** If the error is 400/422 and backend returns field-specific errors (e.g., `error.message` contains a field name or backend returns structured validation errors), call `form.setFields([{ name: fieldName, errors: [message] }])` to display inline validation errors on the corresponding form field. Parse the backend error message to extract field name and validation message.
    - For non-field errors (409 conflict like company name exists, 500 server error, 404 not found): use `message.error(getErrorMessage(error as ApiError))` toast notification.
    - Example pattern:
      ```typescript
      onError: (error) => {
        const apiError = error as ApiError;
        if ((apiError.code === 400 || apiError.code === 422) && apiError.message) {
          // Attempt to parse field-level errors and display inline
          // Backend may return "companyName must not be empty" style messages
          const fieldMatch = parseFieldError(apiError.message);
          if (fieldMatch) {
            form.setFields([{ name: fieldMatch.field, errors: [fieldMatch.message] }]);
            return;
          }
        }
        message.error(getErrorMessage(apiError));
      }
      ```
    - Create a small helper `parseFieldError(msg: string): { field: string; message: string } | null` that attempts to extract field names from validation error messages. If the backend returns messages like "companyName should not be empty" or "联系人电话格式不正确", map known field names to form field names.

    **4. Loading States:**
    - SupplierListPage: ensure table shows `loading={isLoading}` prop from TanStack Query
    - SupplierFormPage: add `loading={createMutation.isPending || updateMutation.isPending}` to submit button
    - SupplierDetailPage: show `LoadingSpinner` or `Skeleton` while data loads (`isLoading` from useQuery)
    - Delete button: add `loading={deleteMutation.isPending}` and `disabled={deleteMutation.isPending}`

    **5. Empty States (SupplierListPage.tsx):**
    - Add `locale={{ emptyText: <Empty description="暂无供应商数据" image={Empty.PRESENTED_IMAGE_SIMPLE}><Button type="primary" icon={<PlusOutlined />} onClick={() => navigate('/suppliers/new')}>新建供应商</Button></Empty> }}` to the Table component

    **6. Detail Page Completeness (SupplierDetailPage.tsx):**
    - Cross-reference all fields from backend response against rendered fields
    - Ensure loading state, error state, and not-found state are all handled
    - Error handling on data fetch failure should use `getErrorMessage()`

    **7. Update Regression Tests:**
    - Update `SupplierListPage.test.tsx`: add tests for empty state rendering, error message display on delete failure (mock ApiError with code 409), search with `companyName` parameter
    - Update `SupplierFormPage.test.tsx`: add test for error message display on submit failure, add test for inline field validation error display via form.setFields() when backend returns 400/422
    - Update `SupplierDetailPage.test.tsx`: add test for loading state, error state display

    **8. Backend DTO Fix (if needed per audit):**
    - If audit identifies that `QuerySupplierDto` needs a `keyword` field for unified search, add `@IsOptional() @IsString() keyword?: string` and implement search logic in service
    - OR if audit decides frontend adapts, ensure frontend sends `companyName` (not `keyword`)
    - Follow the audit's direction (frontend/backend/both) for each specific bug

    Run after all fixes: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck`
    If backend changed: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm build && pnpm test && pnpm lint`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/suppliers/SupplierListPage.tsx` returns >= 1
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/suppliers/SupplierFormPage.tsx` returns >= 1
    - `grep -c "form.setFields\|setFields" frontend/src/pages/suppliers/SupplierFormPage.tsx` returns >= 1 (inline field validation)
    - `grep "error\.response\?\.status" frontend/src/pages/suppliers/SupplierListPage.tsx` returns NO matches (exit code 1)
    - `grep "error\.response\?\.status" frontend/src/pages/suppliers/SupplierFormPage.tsx` returns NO matches (exit code 1)
    - `grep -c "Empty" frontend/src/pages/suppliers/SupplierListPage.tsx` returns >= 1 (empty state component)
    - `grep -c "loading" frontend/src/pages/suppliers/SupplierListPage.tsx` returns >= 1 (loading state)
    - `grep -c "companyName\|keyword" frontend/src/api/supplier.api.ts` confirms search parameter is `companyName` (not `keyword` unless backend was changed)
    - `cd frontend && pnpm test -- --run src/pages/suppliers/__tests__/` exits 0
    - `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All Supplier module pages work correctly: list page searches by companyName, delete shows Chinese error messages via getErrorMessage, form shows specific error on submit failure with inline field validation for 400/422 errors via form.setFields(), empty state renders with action button, loading states visible on all async operations. All supplier tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Customer Module (List + Form + Detail Pages)</name>
  <files>
    frontend/src/pages/customers/CustomerListPage.tsx
    frontend/src/pages/customers/CustomerFormPage.tsx
    frontend/src/pages/customers/CustomerDetailPage.tsx
    frontend/src/api/customer.api.ts
    frontend/src/hooks/queries/useCustomers.ts
    frontend/src/pages/customers/__tests__/CustomerListPage.test.tsx
    frontend/src/pages/customers/__tests__/CustomerFormPage.test.tsx
    frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx
    backend/src/customer/dto/query-customer.dto.ts
  </files>
  <read_first>
    .planning/phases/01-frontend-bug-fixes/01-AUDIT.md
    frontend/src/pages/customers/CustomerListPage.tsx
    frontend/src/pages/customers/CustomerFormPage.tsx
    frontend/src/pages/customers/CustomerDetailPage.tsx
    frontend/src/api/customer.api.ts
    frontend/src/hooks/queries/useCustomers.ts
    frontend/src/types/api.types.ts
    frontend/src/types/entities.types.ts
    frontend/src/utils/errorMessages.ts
    frontend/src/pages/customers/__tests__/CustomerListPage.test.tsx
    frontend/src/pages/customers/__tests__/CustomerFormPage.test.tsx
    frontend/src/pages/customers/__tests__/CustomerDetailPage.test.tsx
    backend/src/customer/customer.controller.ts
    backend/src/customer/dto/query-customer.dto.ts
    frontend/src/pages/suppliers/SupplierListPage.tsx
    frontend/src/pages/suppliers/SupplierFormPage.tsx
  </read_first>
  <action>
    Apply the SAME fix pattern established in Task 1 for Supplier, now to the Customer module. Consult 01-AUDIT.md for Customer-specific bugs.

    **1. Search Parameter Fix (CustomerListPage.tsx):**
    - Change frontend search form to send `companyName` parameter instead of `keyword`
    - Mirror the pattern used in SupplierListPage (read the fixed SupplierListPage as reference)

    **2. Error Handling Fix (CustomerListPage.tsx + CustomerFormPage.tsx):**
    - Replace ALL `error.response?.status` with `error.code`
    - Import `getErrorMessage`, `getDeleteErrorMessage` from `@/utils/errorMessages`
    - Delete handler: `getDeleteErrorMessage(error, '客户')`
    - Form submit: `message.error(getErrorMessage(error as ApiError))`

    **3. Inline Field Validation (CustomerFormPage.tsx) -- per user decision:**
    - **CRITICAL (locked decision):** Same pattern as SupplierFormPage -- if error is 400/422 and backend returns field-specific errors, call `form.setFields([{ name: fieldName, errors: [message] }])` to display inline validation errors on the corresponding form field. Reserve `message.error()` for non-field errors (409, 500, 404).
    - Reuse the `parseFieldError` helper created in SupplierFormPage (or extract to a shared utility if not already done).

    **4. Loading States:**
    - CustomerListPage: `loading={isLoading}` on Table
    - CustomerFormPage: `loading={createMutation.isPending || updateMutation.isPending}` on submit button
    - CustomerDetailPage: `LoadingSpinner` or `Skeleton` while loading

    **5. Empty States (CustomerListPage.tsx):**
    - Table `locale={{ emptyText: <Empty description="暂无客户数据" image={Empty.PRESENTED_IMAGE_SIMPLE}><Button type="primary" icon={<PlusOutlined />} onClick={() => navigate('/customers/new')}>新建客户</Button></Empty> }}`

    **6. Detail Page (CustomerDetailPage.tsx):**
    - Verify all fields rendered match backend response
    - Add proper error/loading states
    - Note: CustomerDetailPage is 658 lines -- DO NOT refactor it (Phase 4 scope), only fix bugs

    **7. Update Regression Tests:**
    - Mirror test updates from Supplier module
    - CustomerListPage.test.tsx: empty state, delete error, search param tests
    - CustomerFormPage.test.tsx: submit error message test, inline field validation test for 400/422 errors via form.setFields()
    - CustomerDetailPage.test.tsx: loading and error state tests

    **8. Backend DTO Fix (if needed per audit):**
    - Same direction as Supplier -- if audit says add `keyword` to backend, do it; if frontend adapts, use `companyName`

    Run: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck`
    If backend changed: `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/backend && pnpm build && pnpm test && pnpm lint`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/customers/CustomerListPage.tsx` returns >= 1
    - `grep -c "getErrorMessage\|getDeleteErrorMessage" frontend/src/pages/customers/CustomerFormPage.tsx` returns >= 1
    - `grep -c "form.setFields\|setFields" frontend/src/pages/customers/CustomerFormPage.tsx` returns >= 1 (inline field validation)
    - `grep "error\.response\?\.status" frontend/src/pages/customers/CustomerListPage.tsx` returns NO matches (exit code 1)
    - `grep "error\.response\?\.status" frontend/src/pages/customers/CustomerFormPage.tsx` returns NO matches (exit code 1)
    - `grep -c "Empty" frontend/src/pages/customers/CustomerListPage.tsx` returns >= 1
    - `grep -c "loading" frontend/src/pages/customers/CustomerListPage.tsx` returns >= 1
    - `cd frontend && pnpm test -- --run src/pages/customers/__tests__/` exits 0
    - `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All Customer module pages work correctly: list page searches by companyName, delete shows Chinese error messages, form shows specific errors with inline field validation for 400/422 errors via form.setFields(), empty state with action button, loading states on all async ops. All customer tests pass. Pattern identical to fixed Supplier module.</done>
</task>

</tasks>

<verification>
1. Both Supplier and Customer modules use `getErrorMessage`/`getDeleteErrorMessage` from errorMessages utility
2. No remaining `error.response?.status` patterns in any Supplier or Customer page
3. All list pages have Empty state components and loading states
4. All form pages have loading buttons, specific error messages, and inline field validation via form.setFields() for 400/422 errors
5. Full frontend suite passes: `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck`
6. If backend DTOs changed, backend suite also passes: `cd backend && pnpm build && pnpm test && pnpm lint`
</verification>

<success_criteria>
- Supplier and Customer modules have zero broken error handling patterns
- Search filters work correctly on both list pages
- Form pages display field-level validation errors inline via form.setFields() for 400/422 responses
- Empty states guide users to create first entity
- Loading states visible on every async operation
- All existing + new tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-bug-fixes/01-02-SUMMARY.md`
</output>
