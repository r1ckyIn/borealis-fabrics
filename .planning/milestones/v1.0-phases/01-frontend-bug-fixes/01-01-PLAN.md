---
phase: 01-frontend-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-frontend-bug-fixes/01-AUDIT.md
  - frontend/src/utils/errorMessages.ts
  - frontend/src/utils/__tests__/errorMessages.test.ts
autonomous: true
requirements: [BUGF-05, BUGF-06]
must_haves:
  truths:
    - "A comprehensive audit document exists listing every frontend-backend mismatch with severity, module, and fix approach"
    - "An error code mapping utility exists that converts ApiError codes into user-friendly Chinese messages"
    - "Error mapping utility has test coverage for all defined error codes plus fallback behavior"
    - "When a user triggers an error (e.g., deleting a supplier with related fabrics), the system returns a Chinese message like '该供应商有关联的面料，无法删除' instead of a raw error code"
  artifacts:
    - path: ".planning/phases/01-frontend-bug-fixes/01-AUDIT.md"
      provides: "Complete bug list with ID, severity, module, description, root cause, affected files, fix approach"
      contains: "BUGF-"
    - path: "frontend/src/utils/errorMessages.ts"
      provides: "Error code mapping table and getErrorMessage() utility function"
      exports: ["getErrorMessage", "ERROR_CODE_MESSAGES"]
    - path: "frontend/src/utils/__tests__/errorMessages.test.ts"
      provides: "Unit tests for error message utility"
      contains: "getErrorMessage"
  key_links:
    - from: "frontend/src/utils/errorMessages.ts"
      to: "frontend/src/types/api.types.ts"
      via: "imports ApiError type"
      pattern: "import.*ApiError"
---

<objective>
Produce a comprehensive frontend-backend audit and create the shared error message infrastructure that all subsequent bug fix plans depend on.

Purpose: The audit is a locked user decision -- no fixes begin until every bug is cataloged with severity, root cause, and fix approach. The error utility provides the shared infrastructure needed by all page-level error handling fixes in Plans 02-03.

Output: 01-AUDIT.md (bug catalog) + errorMessages.ts (utility) + tests
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-bug-fixes/01-CONTEXT.md
@.planning/phases/01-frontend-bug-fixes/01-RESEARCH.md

<interfaces>
<!-- Key types the executor needs for error utility -->

From frontend/src/types/api.types.ts:
```typescript
export interface ApiError {
  code: number;
  message: string;
  data: null;
}
```

From frontend/src/api/client.ts (error interceptor output):
```typescript
const apiError: ApiError = {
  code: error.response?.data?.code ?? error.response?.status ?? 500,
  message: error.response?.data?.message ?? error.message ?? 'Unknown error',
  data: null,
};
return Promise.reject(apiError);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Frontend-Backend Audit</name>
  <files>.planning/phases/01-frontend-bug-fixes/01-AUDIT.md</files>
  <read_first>
    frontend/src/api/client.ts
    frontend/src/api/supplier.api.ts
    frontend/src/api/customer.api.ts
    frontend/src/api/fabric.api.ts
    frontend/src/api/quote.api.ts
    frontend/src/api/order.api.ts
    frontend/src/api/import.api.ts
    frontend/src/api/logistics.api.ts
    frontend/src/api/file.api.ts
    frontend/src/types/forms.types.ts
    frontend/src/types/api.types.ts
    frontend/src/pages/suppliers/SupplierListPage.tsx
    frontend/src/pages/suppliers/SupplierFormPage.tsx
    frontend/src/pages/suppliers/SupplierDetailPage.tsx
    frontend/src/pages/customers/CustomerListPage.tsx
    frontend/src/pages/customers/CustomerFormPage.tsx
    frontend/src/pages/customers/CustomerDetailPage.tsx
    frontend/src/pages/fabrics/FabricListPage.tsx
    frontend/src/pages/fabrics/FabricFormPage.tsx
    frontend/src/pages/fabrics/FabricDetailPage.tsx
    frontend/src/pages/quotes/QuoteListPage.tsx
    frontend/src/pages/quotes/QuoteFormPage.tsx
    frontend/src/pages/quotes/QuoteDetailPage.tsx
    frontend/src/pages/orders/OrderListPage.tsx
    frontend/src/pages/orders/OrderFormPage.tsx
    frontend/src/pages/orders/OrderDetailPage.tsx
    frontend/src/pages/import/ImportPage.tsx
    backend/src/import/import.controller.ts
    backend/src/supplier/dto/query-supplier.dto.ts
    backend/src/customer/dto/query-customer.dto.ts
    backend/src/fabric/dto/query-fabric.dto.ts
    backend/src/order/dto/query-order.dto.ts
    backend/src/quote/dto/query-quote.dto.ts
    backend/src/logistics/dto/query-logistics.dto.ts
    backend/src/supplier/supplier.controller.ts
    backend/src/customer/customer.controller.ts
    backend/src/fabric/fabric.controller.ts
    backend/src/order/order.controller.ts
    backend/src/quote/quote.controller.ts
    backend/src/logistics/logistics.controller.ts
    backend/src/auth/auth.controller.ts
  </read_first>
  <action>
    Systematically compare every frontend API call against every backend controller endpoint. For each page (list, form, detail) in every module (Supplier, Customer, Fabric, Quote, Order, Logistics, Import), check:

    1. **API path alignment**: Does the frontend API file path match the backend `@Controller()` prefix + `@Get/@Post/@Patch/@Delete()` path?
    2. **Search parameter alignment**: Does the frontend search form field names match the backend `QueryXxxDto` field names? (e.g., frontend `keyword` vs backend `companyName`)
    3. **Error handling pattern**: Does the page catch errors using `error.code` (correct) or `error.response?.status` (broken)?
    4. **Delete handling**: Does the delete mutation handle 204 No Content correctly?
    5. **Form submit error display**: Does the page show backend error details or generic messages?
    6. **Loading states**: Does the page show loading indicators for async operations?
    7. **Empty states**: Does the list page show Empty component when no data?
    8. **Detail page completeness**: Does the detail page render all fields from the backend response?

    Output format for 01-AUDIT.md:

    ```markdown
    # Phase 1: Frontend Bug Fixes - Audit Report
    **Audited:** {date}
    **Auditor:** Claude (automated scan)

    ## Summary
    | Severity | Count |
    |----------|-------|
    | P0 (Completely Broken) | N |
    | P1 (Feature Abnormal) | N |
    | P2 (UX Issue) | N |

    ## API Contract Specification
    ### Paginated Response Format
    All paginated endpoints MUST return: `{ items: T[], pagination: { page, pageSize, total, totalPages } }`

    ### Error Response Format
    All error responses follow: `{ code: number, message: string, data: null }`

    ## Bug List

    ### BUG-001: [Title]
    | Field | Value |
    |-------|-------|
    | Severity | P0/P1/P2 |
    | Module | Supplier/Customer/etc. |
    | Page | SupplierListPage/etc. |
    | Description | What's broken |
    | Root Cause | Why it's broken |
    | Affected Files | frontend/src/... backend/src/... |
    | Fix Approach | Specific fix steps |
    | Fix Direction | frontend/backend/both |

    (repeat for each bug)

    ## Discrepancy Matrix
    (table showing frontend API call vs backend endpoint for every operation)
    ```

    Also include a section "API Discrepancy Matrix" that shows for every module:
    - Frontend API function -> Backend endpoint -> Match status -> Notes

    Include a "Fix Priority Order" section listing bugs in the order they should be fixed (following business flow: Supplier -> Customer -> Fabric -> Quote -> Order -> Logistics -> Import).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-frontend-bug-fixes/01-AUDIT.md && grep -q '## Summary' .planning/phases/01-frontend-bug-fixes/01-AUDIT.md && [ $(grep -c 'BUG-' .planning/phases/01-frontend-bug-fixes/01-AUDIT.md) -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - 01-AUDIT.md exists at `.planning/phases/01-frontend-bug-fixes/01-AUDIT.md`
    - File contains "## Summary" section with P0/P1/P2 counts
    - File contains "## API Contract Specification" section
    - File contains at least 5 "BUG-" entries (research already found 9 mismatches)
    - Each BUG entry has: Severity, Module, Page, Description, Root Cause, Affected Files, Fix Approach, Fix Direction
    - File contains "## Discrepancy Matrix" showing all frontend-backend API comparisons
    - File contains "## Fix Priority Order" section following business flow order
    - The import controller P0 bug (missing api/v1 prefix) is listed
    - The error handling pattern bug (error.response?.status) is listed for all 4+ affected pages
    - The search parameter mismatches are listed for supplier, customer, and fabric modules
  </acceptance_criteria>
  <done>Comprehensive audit document exists cataloging every frontend-backend mismatch with severity, module assignment, root cause, and fix approach. Audit covers all 7 modules and all 3 page types per module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Error Code Mapping Utility</name>
  <files>frontend/src/utils/errorMessages.ts, frontend/src/utils/__tests__/errorMessages.test.ts</files>
  <read_first>
    frontend/src/types/api.types.ts
    frontend/src/api/client.ts
    frontend/src/utils/constants.ts
    .planning/phases/01-frontend-bug-fixes/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: getErrorMessage returns mapped Chinese string for known error code "CUSTOMER_HAS_ORDERS" -> "该客户有关联的订单，无法删除"
    - Test 2: getErrorMessage returns mapped Chinese string for known error code "SUPPLIER_HAS_FABRICS" -> "该供应商有关联的面料，无法删除"
    - Test 3: getErrorMessage falls back to ApiError.message when code is unknown but message is a readable string
    - Test 4: getErrorMessage returns generic fallback "操作失败，请稍后重试" when message is empty/null
    - Test 5: getErrorMessage handles HTTP status codes: 404 -> "记录不存在或已被删除", 409 -> "存在关联数据，无法删除", 403 -> "没有权限执行此操作", 500 -> "服务器错误，请稍后重试"
    - Test 6: ERROR_CODE_MESSAGES contains at least 10 business error codes
    - Test 7: getDeleteErrorMessage returns specific Chinese message for delete-related errors (409 with context)
  </behavior>
  <action>
    Create `frontend/src/utils/errorMessages.ts` with:

    1. `ERROR_CODE_MESSAGES` record mapping business error codes to Chinese messages:
       ```typescript
       export const ERROR_CODE_MESSAGES: Record<string, string> = {
         SUPPLIER_HAS_ORDERS: '该供应商有关联的订单，无法删除',
         SUPPLIER_HAS_FABRICS: '该供应商有关联的面料，无法删除',
         CUSTOMER_HAS_ORDERS: '该客户有关联的订单，无法删除',
         CUSTOMER_HAS_QUOTES: '该客户有关联的报价单，无法删除',
         FABRIC_HAS_ORDERS: '该面料有关联的订单，无法删除',
         FABRIC_CODE_EXISTS: '面料编码已存在',
         COMPANY_NAME_EXISTS: '公司名称已存在',
         QUOTE_ALREADY_CONVERTED: '该报价单已转为订单',
         QUOTE_EXPIRED: '该报价单已过期',
         INVALID_STATUS_TRANSITION: '当前状态不允许此操作',
         ORDER_HAS_PAYMENTS: '该订单有付款记录，无法删除',
         NOT_IMPLEMENTED: '该功能暂未实现',
       };
       ```

    2. `HTTP_STATUS_MESSAGES` record for common HTTP status codes:
       ```typescript
       export const HTTP_STATUS_MESSAGES: Record<number, string> = {
         400: '请求参数错误',
         401: '请先登录',
         403: '没有权限执行此操作',
         404: '记录不存在或已被删除',
         409: '存在关联数据，无法删除',
         422: '数据验证失败',
         429: '操作过于频繁，请稍后重试',
         500: '服务器错误，请稍后重试',
         501: '该功能暂未实现',
         503: '服务暂时不可用，请稍后重试',
       };
       ```

    3. `getErrorMessage(error: ApiError): string` function:
       - First check if `error.message` matches a key in `ERROR_CODE_MESSAGES` -> return mapped message
       - Then check if `error.code` matches a key in `HTTP_STATUS_MESSAGES` -> return mapped message
       - Then fall back to `error.message` if it's a non-empty string
       - Final fallback: '操作失败，请稍后重试'

    4. `getDeleteErrorMessage(error: ApiError, entityName: string): string` convenience function:
       - For 409: returns "存在关联数据，无法删除" (or specific message from ERROR_CODE_MESSAGES if matched)
       - For 404: returns `${entityName}不存在或已被删除`
       - Otherwise: delegates to `getErrorMessage`

    Import `ApiError` type from `@/types`. Use English comments only.

    Create test file at `frontend/src/utils/__tests__/errorMessages.test.ts` following TDD RED-GREEN-REFACTOR:
    - Write all tests first (RED)
    - Implement the utility (GREEN)
    - Refactor if needed

    Run `cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm test -- --run src/utils/__tests__/errorMessages.test.ts` to verify.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/borealis-fabrics/frontend && pnpm test -- --run src/utils/__tests__/errorMessages.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/utils/errorMessages.ts` exists and exports `getErrorMessage`, `getDeleteErrorMessage`, `ERROR_CODE_MESSAGES`, `HTTP_STATUS_MESSAGES`
    - `ERROR_CODE_MESSAGES` contains at least 10 entries (verify: `grep -c ":" frontend/src/utils/errorMessages.ts` returns >= 20)
    - `getErrorMessage` function signature: `(error: ApiError): string`
    - `getDeleteErrorMessage` function signature: `(error: ApiError, entityName: string): string`
    - Test file exists at `frontend/src/utils/__tests__/errorMessages.test.ts`
    - Tests pass: `pnpm test -- --run src/utils/__tests__/errorMessages.test.ts` exits 0
    - Full frontend suite still passes: `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Error code mapping utility and convenience functions exist with full test coverage. All known business error codes map to Chinese messages. HTTP status codes have user-friendly Chinese fallbacks. Full frontend build+test+lint+typecheck passes.</done>
</task>

</tasks>

<verification>
1. `01-AUDIT.md` exists and contains categorized bugs with P0/P1/P2 severity
2. `frontend/src/utils/errorMessages.ts` exports `getErrorMessage` and related utilities
3. All error message tests pass
4. Full frontend suite passes: `cd frontend && pnpm build && pnpm test && pnpm lint && pnpm typecheck`
</verification>

<success_criteria>
- Comprehensive audit with 5+ bugs documented, each with severity/module/root-cause/fix-approach
- Error mapping utility with 10+ business error codes and HTTP status fallbacks
- All tests pass including new error message tests
- No regressions in existing frontend test suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-bug-fixes/01-01-SUMMARY.md`
</output>
