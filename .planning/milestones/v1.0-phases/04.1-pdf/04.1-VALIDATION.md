---
phase: 04.1
slug: pdf
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-24
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Backend)** | Jest 30 |
| **Framework (Frontend)** | Vitest 4 |
| **Config file (Backend)** | `backend/package.json` jest section |
| **Config file (Frontend)** | `frontend/vite.config.ts` |
| **Quick run command (Backend)** | `cd backend && pnpm test -- --testPathPattern=order-payment` |
| **Quick run command (Frontend)** | `cd frontend && pnpm test -- --testPathPattern=payment` |
| **Full suite command (Backend)** | `cd backend && pnpm test` |
| **Full suite command (Frontend)** | `cd frontend && pnpm test` |
| **Estimated runtime** | ~30 seconds (backend) + ~20 seconds (frontend) |

---

## Sampling Rate

- **After every task commit:** Run quick run command for affected layer (backend/frontend)
- **After every plan wave:** Run full suite for both backend and frontend
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 50 seconds

---

## Per-Task Verification Map

| Behavior | Test Type | Automated Command | File Exists? | Status |
|----------|-----------|-------------------|-------------|--------|
| Backend rejects payment update without voucherFileIds | unit | `cd backend && pnpm test -- --testPathPattern=order-payment` | Partial | ⬜ pending |
| PaymentRecord + PaymentVoucher created in transaction | unit | `cd backend && pnpm test -- --testPathPattern=order-payment` | Partial | ⬜ pending |
| Append-only: no delete endpoint for vouchers | unit | `cd backend && pnpm test -- --testPathPattern=order-payment` | No | ⬜ pending |
| DTO validation: voucherFileIds is non-empty array | unit | `cd backend && pnpm test -- --testPathPattern=update-customer-payment` | No | ⬜ pending |
| Frontend disables save when no vouchers | integration | `cd frontend && pnpm test -- --testPathPattern=payment-flow` | Partial | ⬜ pending |
| VoucherUploader accepts images + PDF + Office | unit | `cd frontend && pnpm test -- --testPathPattern=VoucherUploader` | No - W0 | ⬜ pending |
| VoucherList renders file type icons correctly | unit | `cd frontend && pnpm test -- --testPathPattern=VoucherList` | No - W0 | ⬜ pending |
| Image preview opens modal | integration | `cd frontend && pnpm test -- --testPathPattern=payment-flow` | No | ⬜ pending |
| Word MIME types added to allowed list | unit | `cd backend && pnpm test -- --testPathPattern=file` | Partial | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `frontend/src/components/business/__tests__/VoucherUploader.test.tsx` — stubs for upload component
- [ ] `frontend/src/components/business/__tests__/VoucherList.test.tsx` — stubs for list display
- [ ] No new framework install needed — Jest and Vitest already configured

*Existing infrastructure covers most requirements; Wave 0 adds test stubs for new components.*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Drag-and-drop upload UX | Requires real DOM drag events | Drop a file onto the upload area, verify it appears in the file list |
| PDF opens in new browser tab | Requires real browser tab | Click a PDF voucher link, verify new tab opens |
| Upload progress indicator | Requires real network latency | Upload a large file, verify progress bar appears |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 50s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
