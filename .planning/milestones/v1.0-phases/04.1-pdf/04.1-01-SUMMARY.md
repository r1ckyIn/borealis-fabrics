---
phase: 04.1-pdf
plan: 01
subsystem: payments
tags: [prisma, nestjs, payment-voucher, file-upload, transaction]

requires:
  - phase: none
    provides: "Existing PaymentRecord model and order payment service"
provides:
  - "PaymentVoucher join table linking files to payment records"
  - "Word document (.doc/.docx) file upload support"
  - "Mandatory voucherFileIds validation on payment DTOs"
  - "Transactional PaymentRecord + PaymentVoucher creation"
  - "GET /orders/:id/payment-vouchers endpoint"
affects: [04.1-02, 04.1-03, frontend-payment-pages]

tech-stack:
  added: []
  patterns: ["Transactional audit trail (PaymentRecord + PaymentVoucher in $transaction)"]

key-files:
  created:
    - "backend/prisma/migrations/20260324034038_add_payment_voucher_table/migration.sql"
    - "backend/src/order/dto/update-customer-payment.dto.spec.ts"
    - "backend/src/order/dto/update-supplier-payment.dto.spec.ts"
  modified:
    - "backend/prisma/schema.prisma"
    - "backend/src/common/constants/file.constants.ts"
    - "backend/src/file/file.controller.ts"
    - "backend/src/order/dto/update-customer-payment.dto.ts"
    - "backend/src/order/dto/update-supplier-payment.dto.ts"
    - "backend/src/order/order-payment.service.ts"
    - "backend/src/order/order-payment.service.spec.ts"
    - "backend/src/order/order.controller.ts"
    - "backend/src/order/order.includes.ts"

key-decisions:
  - "FileController uses shared constants from file.constants.ts instead of local duplicates"
  - "voucherFileIds uses definite assignment assertion (!) for TypeScript strict mode compatibility"

patterns-established:
  - "PaymentRecord + PaymentVoucher atomic creation in Prisma $transaction for all payment operations"
  - "Shared file constants imported across modules instead of local duplicates"

requirements-completed: [PVOU-01, PVOU-02, PVOU-03, PVOU-06]

duration: 9min
completed: 2026-03-24
---

# Phase 04.1 Plan 01: Backend Payment Voucher Summary

**PaymentVoucher schema + transactional audit trail + mandatory voucher validation on both customer/supplier payment DTOs with Word doc support**

## Performance

- **Duration:** 9 min
- **Started:** 2026-03-24T03:38:10Z
- **Completed:** 2026-03-24T03:47:34Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments
- PaymentVoucher join table created via Prisma migration linking files to payment records
- Both payment DTOs now require non-empty voucherFileIds array (mandatory voucher upload)
- updateCustomerPayment and updateSupplierPayment create PaymentRecord + PaymentVouchers atomically in $transaction
- New GET /orders/:id/payment-vouchers endpoint returns full payment history with attached files
- Word document support (.doc/.docx) added to file upload constants
- FileController deduplicated - now imports shared constants instead of local copies

## Task Commits

Each task was committed atomically:

1. **Task 1: Schema migration + file constants + DTO updates** (TDD)
   - `7ecc230` (test: failing DTO validation tests)
   - `a42461f` (feat: schema + constants + DTOs)
2. **Task 2: Payment service voucher logic + controller endpoint + tests** (TDD)
   - `dc840b6` (test: failing service tests)
   - `a04a601` (feat: service + controller + includes)

## Files Created/Modified
- `backend/prisma/schema.prisma` - Added PaymentVoucher model + relations on PaymentRecord and File
- `backend/prisma/migrations/20260324034038_add_payment_voucher_table/` - Migration SQL
- `backend/src/common/constants/file.constants.ts` - Added Word MIME types and extensions
- `backend/src/file/file.controller.ts` - Replaced local constants with shared imports
- `backend/src/order/dto/update-customer-payment.dto.ts` - Added voucherFileIds (required) + voucherRemarks (optional)
- `backend/src/order/dto/update-supplier-payment.dto.ts` - Added voucherFileIds (required) + voucherRemarks (optional)
- `backend/src/order/dto/update-customer-payment.dto.spec.ts` - 5 DTO validation tests
- `backend/src/order/dto/update-supplier-payment.dto.spec.ts` - 5 DTO validation tests
- `backend/src/order/order-payment.service.ts` - Transactional PaymentRecord + voucher creation for both payment types + getPaymentVouchers
- `backend/src/order/order-payment.service.spec.ts` - 17 tests covering voucher creation, atomicity, remarks
- `backend/src/order/order.controller.ts` - Added GET :id/payment-vouchers endpoint
- `backend/src/order/order.includes.ts` - Added PAYMENT_RECORD_INCLUDE_VOUCHERS constant

## Decisions Made
- FileController uses shared constants from file.constants.ts instead of maintaining local duplicates (Rule 1 - deduplication)
- voucherFileIds uses TypeScript definite assignment assertion (!) since class-validator DTOs are populated by the framework, not constructors

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] TypeScript strict mode definite assignment error**
- **Found during:** Task 1 (DTO updates)
- **Issue:** `voucherFileIds: number[]` without initializer caused TS2564 in strict mode
- **Fix:** Changed to `voucherFileIds!: number[]` with definite assignment assertion
- **Files modified:** update-customer-payment.dto.ts, update-supplier-payment.dto.ts
- **Verification:** pnpm build exits 0
- **Committed in:** a42461f (Task 1 commit)

**2. [Rule 1 - Bug] ESLint no-unsafe-member-access in test assertions**
- **Found during:** Task 2 (service tests)
- **Issue:** `mock.calls[0][0]` triggers @typescript-eslint/no-unsafe-member-access
- **Fix:** Replaced with direct `toHaveBeenCalledWith` assertions using exact expected values
- **Files modified:** order-payment.service.spec.ts
- **Verification:** pnpm lint exits with 0 errors (only pre-existing warnings in unrelated e2e file)
- **Committed in:** a04a601 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (2 bugs)
**Impact on plan:** Both auto-fixes necessary for build/lint compliance. No scope creep.

## Issues Encountered
- Jest 30+ `--testPathPattern` flag is deprecated, must use `--testPathPatterns` (already documented in project CLAUDE.md)
- pnpm test wrapper uses old flag syntax, direct `npx jest --testPathPatterns` works correctly

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Backend API ready for frontend integration (Plan 02: frontend components)
- PaymentVoucher data model supports append-only design (no delete endpoint)
- All 29 test suites (676 tests) pass, build clean, lint clean

## Self-Check: PASSED

All 13 files verified present. All 4 commits verified in git history.

---
*Phase: 04.1-pdf*
*Completed: 2026-03-24*
