---
paths:
  - "**/backend/**"
  - "**/frontend/**"
  - "**/src/**"
---

# 项目开发流程（继承 + 覆盖）

> **继承规则**：本文件继承父级 `r1ckyIn_GitHub/.claude/rules/01-workflow/development-cycle.md` 的全部规则
> （变体 A-E、Git 分支策略、失败处理、单功能开发周期等）。
> 以下仅列出**覆盖**和**项目特定**内容。

---

## 覆盖：开发阶段-工作流速查表

| 阶段 | 子阶段/模块 | 推荐工作流 | 说明 |
|------|-----------|-----------|------|
| **阶段 1** | 1.1 后端基础设施 | 变体 D | 配置文件、依赖安装 |
| | 1.2 Docker 环境 | 变体 D | MySQL + Redis 容器配置 |
| | 1.3 数据库 Schema | 变体 A | Prisma 专用流程 |
| | 1.4 通用模块 | 变体 B | NestJS 模块开发 |
| | 1.5 前端初始化 | 变体 D | Vite + React 项目配置 |
| | 1.6 CI/CD | 变体 D | GitHub Actions 配置 |
| **阶段 2** | SupplierModule | 变体 B | 供应商管理 |
| | CustomerModule | 变体 B | 客户管理（含特殊定价） |
| | FabricModule | 变体 B | 面料管理（含图片 API） |
| | FileModule | 变体 B | 文件上传服务 |
| **阶段 3** | QuoteModule | 变体 B | 报价管理（含编号生成） |
| | 定时任务 - 报价过期 | 变体 E | @nestjs/schedule |
| | OrderModule | 变体 B | 订单管理（含明细 API） |
| | LogisticsModule | 变体 B | 物流管理 |
| | ImportModule | 变体 B | Excel 批量导入 |
| | AuthModule | 变体 B | 企业微信 OAuth |
| | SystemModule | 变体 B | 枚举值、健康检查 |
| **阶段 4** | 前端页面 | 变体 C | React + Ant Design |
| **阶段 5** | 集成测试 | 单功能开发周期 | 完整 TDD 流程 |
| **阶段 6** | 部署上线 | 变体 D | 腾讯云部署配置 |

---

## 覆盖：通用 PR 流程（步骤 5-9）

> 覆盖父级步骤 5-8，在 self-review 前增加 code-simplifier 步骤。

```
┌─────────────────────────────────────────────────────────────────┐
│ 步骤 5：使用 code-simplifier 优化代码（强制）                      │
│ ├── 调用 code-simplifier 插件                                    │
│ ├── 简化复杂逻辑，提升可读性                                       │
│ ├── 移除冗余代码                                                  │
│ └── 确保代码简洁、一致、易维护                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 步骤 6：使用 code-reviewer 进行严格自我代码审查                    │
│ （同父级步骤 5）                                                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 步骤 7：提交到 feature 分支（同父级步骤 6）                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 步骤 8：通过 PR 合并到 main（同父级步骤 7）                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 步骤 9：更新文档（同父级步骤 8）                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 变体微调

- **B1**：阅读 `docs/ARCHITECTURE.md` 确认 API 设计、DTO 结构
- **A1**：阅读 `docs/ARCHITECTURE.md` 确认表结构、字段、枚举

---

## 项目特定信息

### 定时任务列表

| 任务 | 执行时间 | 说明 | 状态 |
|------|---------|------|------|
| QuoteExpirationJob | 每小时 | 检查并标记过期报价（status → expired） | ✅ 已完成 |

### 模块开发顺序

```text
阶段 2：核心数据模块（✅ 已完成）
├── SupplierModule（供应商管理）
├── CustomerModule（客户管理 + 特殊定价）
├── FabricModule（面料管理 + 图片 API）
└── FileModule（文件上传服务）

阶段 3：业务流程模块（🔄 进行中）
├── QuoteModule（报价管理 + 编号生成 + 定时任务） ✅
├── OrderModule（订单管理 + 明细 API） ✅
├── LogisticsModule（物流管理） ✅
├── ImportModule（Excel 批量导入） ✅
├── AuthModule（企业微信 OAuth） ⏳
└── SystemModule（枚举值、健康检查） ⏳
```

---

## 工作流摘要（一行）

```
规划 → TDD 测试 → 实现功能 → Build → Test → Lint → code-simplifier → 代码审查 → /commit → PR → /code-review → 合并 → /clean_gone → 更新文档
```
